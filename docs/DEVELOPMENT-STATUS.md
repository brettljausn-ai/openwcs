# openWCS — Development Status

_Last updated: 2026-07-09 (**In-app help drawer fully localised**: the per-screen help drawer now renders fully translated copy in all five languages (German, French, Spanish, Chinese, Brazilian Portuguese) for every one of the 34 screens (summary, section bodies and tips), closing the earlier gap where only the priority screens were translated and the rest fell back to English; the drawer **title** (the screen name) now resolves through the `nav` namespace in `ui/src/help/HelpButton.tsx` rather than the hardcoded English screen label. The demo bundle in `public/static/demo` was rebuilt. Earlier on 2026-06-23: (**Live-demo hardware visualisation (3D twin) now driven**: the public live demo's **Hardware visualisation** screen now renders a live 3D twin with **totes driving round a recirculating conveyor loop** past an ASRS rack (a crane working it), a sorter and two pick stations, plus roaming AMRs, all client-side. A new `ui/src/demo/demoTwin.ts` single-sources the fixed layout and fabricates the automation topology + device-task feed + twin read-models (tote paths / AMR fleet / ASRS cranes) as pure functions of the wall clock, wired into `mockApi.ts` (`/api/master-data/equipment`, `/api/flow/automation/topology`, `/api/flow/device-tasks`, `/api/flow/twin/{tote-paths,amr-fleet,asrs-cranes}`); it reuses the production twin renderer (no second, cruder scene). The `/live-demo` tour now points at it (`ld.guideTwinNote`, en/de/fr/es parity). The committed demo bundle in `public/static/demo` was rebuilt. Earlier on 2026-06-20: **Live-demo dashboards/reports enriched + a "where to go" tour**: the public live demo's dashboard and report fixtures now carry a fuller curated sample dataset (deterministically generated, ids resolving to the demo SKUs/locations) so the Dashboards and Reporting areas look populated instead of sparse, and the `/live-demo` marketing page gained a numbered walkthrough of the PP1 pick station plus a list of the menus that carry sample data (`ld.guide*` i18n keys in en/de/fr/es, `i18n-check` passes). The committed demo bundle in `public/static/demo` was rebuilt. Earlier on 2026-06-19: **Configurable GTP picking + pick-to-light (PTL) adapter** (epic `feat/gtp-configurable-picking`): GTP stations gained a configurable **pick layout** (`gtp_station.pick_layout` + `pick_slots`, Flyway gtp `V10`): `ONE_TO_ONE` (1 source → 1 target, the classic single carton), `ONE_TO_N` (a batch station with N lit pick slots, N ≥ 2, `pick_slots` = N), or `PUT_WALL` (lit cubbies; only valid when `mode = PUT_WALL`, existing put-wall stations migrated). Validation: `ONE_TO_N` requires `pickSlots >= 2`. `pickLayout` + `pickSlots` are exposed on the StationView / workplace view / WorkCycle view and configured in the GTP config screen (a "Picking layout" selector + a "Pick slots" input shown for `ONE_TO_N`; put-wall cubbies + put-light ids stay per ORDER node). **Operator pick screen renders by layout** (`ui/src/gtpops/GtpOpsScreen.tsx`): the active pick is one box split into three columns — left = source (unchanged), middle = the take quantity (`→ N →`, the sum of OPEN puts), right = `ONE_TO_ONE` a single focused qty input (full / short, as before), `ONE_TO_N` all N slots left-to-right (`SlotsView`: GREY unused, ORANGE in-use with the slot qty, GREEN once picked; tap a slot to confirm, a short inline stepper for a partial), `PUT_WALL` only the needed put lights (orange until picked then green, tap to confirm). Auto-release on completion stays. **Pick-to-light is now physically driven**: a new Go **PTL adapter** (`services/adapters/ptl`, family `PTL`, port **9098**) implementing the uniform device contract (`POST /tasks` `ILLUMINATE`/`CLEAR`, `/healthz` `/readyz` `/state` view of the lit lights, in-memory, with a stub device loop as the real HTTP/serial seam). The gtp service drives it via `LightControlClient` / `HttpLightControlClient`: `ILLUMINATE` each put light on present, `CLEAR` on confirm and on close — **best-effort** with short timeouts so a light failure never blocks the pick — targeting the PTL adapter (`openwcs.gtp.ptl-base-url`, default :9098) or the **equipment-emulator** (`openwcs.gtp.emulator-url`, default :9097, which gained a PTL family tracking lit lights in `/state`) when the hardware-emulator flag is ON. flow-orchestrator adapters map + docker-compose (ptl-adapter on 9098, gtp + flow env) + deploy/k8s updated. The public live demo carries a station of each layout (PP1 ONE_TO_ONE, PP2 ONE_TO_N 7 slots, PW1 PUT_WALL 6 cubbies) and renders the slot/light states (the PTL adapter is not in the frontend-only demo). Earlier: **Process designer — decision step, orthogonal canvas edges, reference process seed**: **Decision step** (`type:"decision"`, `DecisionStep`/`isDecisionStep` in `model.ts`): a no-code, client-evaluated if/elseif/else router; no screen, no server call, no data write — the runtime evaluates ordered `transitions` (first matching `when` wins) and falls back to `next` (else), routing straight through. Authored in `DecisionDialog.tsx` (ordered if/elseif rules + an else target, same condition grammar and variable autocomplete as compute/branch conditions, live validation, move-up/down/remove). Added to `FlowCanvas.tsx` (node badge `◆ decision`, `is-decision-node` class, `stepShortLabel` shows rule count), `SCREEN_TYPE_ICONS`/`SCREEN_TYPE_LABELS`, and the OpenAPI spec (step type enum adds `decision`; `transitions` and `next` semantics documented for the routing case). **Canvas UX**: edges now use **orthogonal routing** (`getSmoothStepPath`, `borderRadius:6`) for cleaner dense graphs; node **delete X** is a fixed always-visible `op-fc-node-del` button (set-start ▶ hidden on the start node); Edit-as-draft and a styled discard dialog added (#343). Fixes: blank screen after Publish (#344); decision rule inputs themed (#344). **Seeded reference process** `stock-count-ref` (Flyway `V4__seed_reference_stock_count.sql`): ACTIVE demo process exercising the full toolkit — verify location, verify SKU, `inventory.lookup` expected on-hand, count capture, `compute` match flag (with recount tolerance), `decision` step that posts (`txlog.post`) on match or loops back for a recount; its own process key, never clobbers `stock-check` or user drafts. Tests: `DecisionStepPublishValidationTest` (publish validation for decision steps), `SeedReferenceTest` (seed present, start/step walkable). i18n de/fr/es/zh. **Public site**: `data-i18n` on `<img>` tags now sets `alt` (not `innerHTML`) in `public/static/i18n.js` (#342). Earlier: **Process designer — canvas-first editor, no-code compute, order/ASN verify, guided dialogs**: a large round of post-Phase-3 work on the configurable handheld process designer. **Visual node canvas** (React Flow, `FlowCanvas.tsx`) is now the **sole** flow editor (the Canvas/List toggle + list view were removed): drag step nodes (positions persist in the model `ui:{x,y}`), draw the flow by dragging connectors (first link = default `next`, further links = `when`-branch edges edited inline on the edge), delete edges, loops detected/shown, orphan nodes render "not connected", a **Tidy** button auto-lays-out the graph; default-next + branches moved off the properties panel onto the canvas. **Definitions landing table** is the entry point (search + New process; pick a row to open the editor, back button). **Guided-dialog UX**: the properties panel is now a per-step summary + skip-when; config opens in dialogs near the live phone preview — Edit screen, Edit task (server task / compute / sandboxed script, searchable task picker, task + input/output descriptions, AI task-assist), Verify flow, Data object. **Verify kinds grew** from `barcode`/`sku`/`location` to also `skuScan` (code-or-barcode + UOM prompt), `order` (outbound order/picksheet barcode) and `asn` (inbound ASN barcode); order/asn resolve via **order-management** `GET /api/orders/resolve?warehouseId=&ref=` → `{found, order{orderId,orderRef,orderType,status,customerRef,lineCount}}` (200 `found:false`, `ORDER_VIEW`); `capabilities.verifyKinds` now lists all six. **Resolved fields can be SCALARS or OBJECTS**: a `verify.write` may store a whole resolved object (`uom`/`sku`/`location`/`order`/`asn`) into a variable or drill into one property (`uom.factor`); the per-kind `VerifyFields` catalog is shared by the designer (`capabilities.verifyFields`), the runtime (`VerifyResult.fields`), and publish validation. **No-code `compute` step**: assign variables from a safe value-returning expression grammar (superset of the condition grammar + arithmetic; client-evaluated; parsed at publish by `ExpressionParser` → 422 on malformed), for decisions/derivations (counted vs expected with a recount tolerance). **Undeclared-variable check**: branch `when`, `skipWhen`, and compute expressions are validated to reference only declared data-object variables (designer + publish). **Curated tasks** carry descriptions + per-input/output descriptions; `inventory.lookup` resolves on-hand by **location OR handling unit** with `warehouseId` auto-injected from the instance. **order-management** added `GET /api/orders/resolve` (`OrderController.resolve` → `ResolveOrderResult`) for scan verification. **Process designer — scan-verification follow-ons**: the `sku` verify kind now also resolves a **barcode input** (barcode resolution first, SKU-code-resolution fallback, so one screen handles both scan sources without a separate `barcode` step); when the resolved SKU carries multiple UOMs the runtime shows an inline **UOM-picker** before merging the verify write-mappings and continuing; the designer's condition, `when`, and verify write-mapping editors gained a **variable autocomplete** (`VarCombobox`) that offers all declared data-object variable names as type-ahead suggestions; the seeded Stock Check sample process was updated with scanned-code output variables in its data object (V3 migration `V3__stock_check_scan_variables.sql`). Designer UX: the flow-step list now renders loop-back arrows for cycle steps, the Properties-panel toolbar was decluttered, and validation error messages were clarified. **Process designer (process-designer, 8097) — scan verification**: text/number input screens gained an optional `config.verify` block `{kind:"barcode"|"sku"|"location", write:{<id|code|name|uomCode|schemaCategory>:<variable>}, onNotFound:{mode:"reprompt"|"goto", step?}}` that confirms a scanned/typed code EXISTS in master data and pulls its linked ids (incl. the resolved UUID) into data-object variables. master-data gained read-only resolve endpoints (`GET /api/master-data/resolve/{sku-by-barcode,sku,location}`, RBAC `MASTER_DATA_VIEW`, 200 `found:false` on a miss, never 404) returning the full barcode → UOMs → SKU → attribute-schema graph (or a location). process-designer added a `POST /api/process-designer/verify {warehouseId, kind, code}` proxy (`PROCESS_DESIGN_VIEW`) that forwards the operator identity and returns a normalised `{found, ambiguous, id, code, name, uomCode, schemaCategory, detail}` (clean `found:false` 200, downstream failure 502), `capabilities.verifyKinds=["barcode","sku","location"]`, publish-time validation (kind valid, write targets declared, goto step exists and counts toward reachability), and config `OPENWCS_MASTER_DATA_BASE_URL`. Frontend: a first-class "Verify the scanned value exists" section on text/number screens; the runtime resolves on submit (offline holds, not-found re-prompts/routes, found merges the writes and continues, ambiguous note), Simulate resolves locally with a "simulate not found" toggle; i18n de/fr/es/zh. Stays on the curated-API path (no raw SQL, RBAC + audit + service boundaries intact) on branch `feat/process-designer-verify`. **Process designer (process-designer, 8097) — Phase 3 (final phase, feature complete)**: the two §7.2/§7.3 tiers shipped, so the configurable handheld process designer is now feature complete through Phase 3. **Sandboxed scripting escape hatch**: a built-in `script` task type (step config `{ script:"<js>", outputs:[{name}] }`) runs server-side in a locked-down GraalJS sandbox (org.graalvm.polyglot community 24.1.1: `allowAllAccess(false)`, `HostAccess.NONE`, no host class lookup/loading, no native/threads/process, `IOAccess.NONE`, no env, `PolyglotAccess.NONE`) — interpreted guest JS, never compiled to host JVM bytecode — with a `ResourceLimits` statement limit (default 100000) + a watchdog wall-clock timeout (default 2000 ms) + a 65536-byte output cap; the script sees only a deep-frozen read-only `data` global and returns an object whose fields become the declared outputs. **Governance** (defense in depth): a new permission `PROCESS_SCRIPT_AUTHOR` (ADMIN only) + a config flag `openwcs.process-designer.scripting.enabled` (default false) — a definition with a `script` step can be saved/published only when both hold (else 422 disabled / 403 no permission), and each script is parse-validated in the sandbox at publish (malformed → 422). **AI task-assist** (design-time only): `POST /api/process-designer/assist/task` `{description, variables:[{name,type}]}` → `{kind:"curated"|"script"|"none", taskType?, inputs?, outputs?, script?, rationale, confidence}` (gated `PROCESS_DESIGN_EDIT`), Anthropic Java SDK (`com.anthropic:anthropic-java` 2.34.0, default `claude-haiku-4-5`, key from `ANTHROPIC_API_KEY`), grounded with the live task catalog + supplied variables; maps to a curated task type or drafts a sandboxed snippet for a human to review — never auto-deployed (AI text → compiled Java → live server is permanently out of scope); absent key → 503 and the context still starts. **Capabilities** endpoint `GET /api/process-designer/capabilities` → `{scriptingEnabled, aiAssistEnabled, canAuthorScript}` (`PROCESS_DESIGN_VIEW`). New compose env: `OPENWCS_PROCESS_SCRIPTING_ENABLED` (default false), `ANTHROPIC_API_KEY` (opt-in), `OPENWCS_PROCESS_ASSIST_MODEL`. **Frontend Phase 3** (`ui/src/process-designer/`): a script-step code editor (shown only when `scriptingEnabled && canAuthorScript`) with a declared-outputs editor + sandbox-limit help, and an AI "describe what this task should do" assist panel (Apply a curated suggestion, or Insert an AI-drafted snippet as a `script` step labelled "AI draft, for your review"); graceful 503 handling; i18n de/fr/es/zh. Phase 3 (backend + frontend) is complete on branch `feat/process-designer-p3`. **Process designer (process-designer, 8097) — Phase 2**: builds on Phase 1 with version management (`POST /defs/{key}/{version}/duplicate` clones a version into a new DRAFT to iterate from a published version; JSON import/export, `POST /defs/import` creates a DRAFT from a full definition JSON and export = `GET /defs/{key}/{version}`, to move defs between environments), step-level `skipWhen` conditions (same restricted grammar as transition `when`; the client runtime skips the step when true, the backend validates it at publish by a new recursive-descent `ConditionParser`, no eval; malformed → 422, and a skippable step must keep an onward path), four more curated task types calling real audited endpoints with forwarded identity (`host.confirm` → `POST /api/host/inventory/adjustments`, `inventory.adjust` → `POST /api/txlog/events` `StockAdjusted`, `counting.capture` → `POST /api/counting/tasks/{taskId}/lines/{lineId}/station-count`, `order.lookup` → `GET /api/orders/{id}`) plus a task-catalog endpoint `GET /api/process-designer/tasks` (`[{type,label,inputs[],outputs[]}]`) driving the designer's task picker, and instance history/monitoring `GET /api/process-designer/instances?processKey=&status=&warehouseId=&limit=` (summaries newest-first, plus the existing `GET /instances/{id}` detail; migration V2 added monitoring indexes; new compose env on process-designer `OPENWCS_INTEGRATION_HOST_BASE_URL`, `OPENWCS_COUNTING_BASE_URL`). **Process designer frontend (Phase 2)** adds a designer version-management pane (drafts/active/archived, only DRAFT editable), a duplicate/clone button, JSON export download + import upload, a "Skip this step when…" condition editor, richer pre-publish validation (unreachable steps, skip-with-no-onward-path, malformed conditions, task steps missing required inputs per the live catalog, duplicate ids), a task picker driven by the live server catalog, and a new read-only desktop **Process instances** monitoring screen at `/process-instances` (Engineering, ADMIN/SUPERVISOR) listing instances + a detail pane (data object, current step, step trail). Phase 2 (backend + frontend) is complete on branch `feat/process-designer-p2`. **RF resilience (Fulfilment integrity + RF resilience epic)**: the mobile runtime now persists **every screen advance** to the server via a durable offline queue (`stepQueue`, IndexedDB) and advances the operator immediately, so a refresh / device switch / RF re-login resumes on the EXACT step with the data intact; the handheld OperatorMenu has a **"Resume in-progress work"** launcher (the operator's RUNNING instances, server-filtered on `assignedTo`); a new supervisor **RF-Users** screen (`process:rf-users`, Engineering, `/rf-users`, ADMIN/SUPERVISOR) lists active operators with their process + current workstep and a **Reassign** action; and a **Replay** viewer steps through what each operator saw and entered, reusing the real `ProcessScreenView` renderer. Backend: per-screen step history (`process_step_event` + `assigned_to` on `process_instance`, migration `V6`), `POST /instances/{id}/step` (idempotent on `instanceId+seq`, sets the exact `current_step`), `GET /instances/{id}/steps`, an `assignedTo` filter on the instance list, `POST /instances/{id}/reassign {toUser}` (supervisor-gated, audited), the operator username injected into every task's inputs, and three new curated fulfilment tasks `picking.book` / `inventory.reduce` / `order.load` (`InstanceStepHistoryTest`, `FulfilmentTasksTest`). **Process designer (process-designer, 8097) — Phase 1**: a new Java microservice — engine + storage for configurable handheld operator processes (Phase 1 of `docs/process-designer-spec.md`). A process is a versioned JSON flow of handheld screens + task steps with **exactly one ACTIVE version per process key** (Postgres partial unique index; versions auto-increment per key, own `process_designer` schema). The handheld client walks the screen flow + branch conditions offline; the server owns def storage/versioning, instance checkpointing for device-swap resume, and **task-step execution**. Definition API under `/api/process-designer/defs` (+ `/defs/{key}/{version}`, `/defs/{key}/active`, `/processes`): create DRAFT, edit DRAFT (409 if not DRAFT), publish (validates step reachability / dangling transitions / `writeTo` + task refs / `start` → 422 with the problem list if invalid, else flips ACTIVE + archives the prior ACTIVE atomically, audited with `X-Auth-User`), archive. Instance API (`/instances`, `/instances/{id}/checkpoint`, `/instances/{id}`): start against the active def (returns the pinned def JSON + start step), checkpoint a task step (**idempotent** on `(instanceId, stepId)` via a `process_step_result` ledger — re-posting returns the stored result without re-running side effects; merges posted data, runs the curated task with the operator's `X-Auth-*` identity forwarded, merges named outputs back, advances `current_step`; a failing task → 502 and does NOT advance), resume. **Curated task library** (server-side registry, no API to author tasks): `inventory.move` → `POST /api/flow/moves`, `slotting.putaway` → `POST /api/slotting/decant/putaway`, `picking.confirm` → `POST /api/orders/pick-tasks/{lineId}/confirm`, `inventory.lookup` → `GET /api/inventory/availability`, `txlog.post` → `POST /api/txlog/events`. Ships a seeded ACTIVE **Stock Check** process (scan location → scan SKU → count → `txlog.post` records a `StockCounted` event → ack). New RBAC permissions `PROCESS_DESIGN_VIEW` (reads + running instances; all shipped roles) and `PROCESS_DESIGN_EDIT` (def writes/publish; supervisor/admin). Wired: gateway route `/api/process-designer/**` (distinct prefix so it does not collide with the Flowable process-engine's `/api/process/**`), compose + k8s, `contracts/openapi/process-designer.yaml`. Tests (Testcontainers + MockMvc): def lifecycle + one-ACTIVE-per-key + invalid-publish 422, instance start/checkpoint/idempotency/resume with identity forwarding + output merge, RBAC, seed. **Process designer frontend (ui)**: a shared `ProcessScreenView` renders the 6 screen types (text/number/date/acknowledge/yes-no/choice) with validation + scan-binding + `{{placeholder}}` resolution (codes via `useCatalog`, no UUIDs); a **client-driven runtime** at `/process/:key` walks the screen flow locally, evaluates branch conditions with a safe TS interpreter and posts only task-step checkpoints (offline-queued, holding at a task when offline); the handheld **operator menu now also lists the active configurable processes** as tiles (alongside the curated `process`-flagged screens); and a **WYSIWYG designer** in Engineering (`/process-design`, ADMIN/SUPERVISOR) with a three-pane layout (flow list + a LIVE handheld preview reusing `ProcessScreenView` + properties panel + data-object panel), a Simulate mode, validate and publish-active. i18n de/fr/es/zh. Phase 1 (backend + frontend) is complete on branch `feat/process-designer-p1`. **PWA operator shell (ui)**: when running as an installed PWA on a handheld (display-mode standalone; testable with `?handheld=1` / `?desktop=1`) the app drops the sidebar / dashboards / master-data for a stripped operator shell with a slim top bar + a **big-tile operator-process menu** (curated `process`-flagged screens, NOT generated from the BPMN designer): **Picking** + **Stock Check** live, **Goods In** + **Packing** coming-soon; each process has a "← Menu" back button, the handheld landing is the menu, non-process URLs redirect to it, and the desktop browser experience is unchanged. **RF picking PWA (ui)**: the frontend is now an **installable PWA** (vite-plugin-pwa: a web manifest + a workbox service worker that precaches the app shell so a Wi-Fi blip never white-screens a handheld; `/api` uses NetworkFirst with a 4 s timeout so picking data is never served stale), and the **Picking screen is now scanner-first** for rugged Android handhelds in keyboard-wedge mode: an auto-refocusing capture input reads wedge scans (keystrokes + Enter) and matches them against the current pick's expected location code, then SKU code (a mismatch shows a big warning + vibrate), auto-confirming at full qty, with a large glove-friendly UI and manual Confirm / Short still available; an **offline confirm queue** (IndexedDB, localStorage fallback) durably holds confirm POSTs that fail on network / 5xx and advances optimistically, retrying on `online` / interval / manual (4xx treated as permanent-failed, 409 as already-done), surfaced by an "Online / Offline, N pending" banner; operators configure the Zebra/Honeywell DataWedge profile for keystroke output with an Enter suffix, and "Install app" to run standalone. Earlier this cycle, execution-layer residuals: four loose ends closed. (1) **destFamily resolution (flow)**: a cross-system `POST /api/flow/moves` now resolves the STORE leg's device family from the destination location's storage block (`storageType` → ASRS/AUTOSTORE/AMR) instead of defaulting to the source family, so a cross-system move into an AutoStore destination correctly issues BIN_STORE (falls back to the source family when the destination block is unresolvable; the prior move-dispatch destFamily caveat is now resolved). (2) **Persisted dispatch-ops entity (order-management)**: a new `route_dispatch` wave table (`V11`, status OPEN→LOADING→DEPARTED + opened/firstLoaded/departed timestamps + assigned/ready counts), lazily upserted and advanced best-effort in-transaction as a route's orders are assigned / released / shipped; `GET /api/orders/reports/dispatch` is now backed by this entity (real status + accurate departure timestamps, same JSON shape) and `GET /api/orders/reports/sla` uses the wave's real `departedAt` vs cut-off (falling back to ship time), so the dispatch tile is now first-class (no longer purely derived). (3) **ASRS crane motion in the twin (emulator + flow + ui)**: emulator `GET /asrs/cranes` telemetry (simulated cranes travelling aisle + lift during ASRS tasks), flow `GET /api/flow/twin/asrs-cranes` read model in world coordinates, and the hardware twin renders ASRS cranes gliding/lifting in the rack with per-frame interpolation (like the AMR fleet): status-coloured, carried HU on hover, legend entry. (4) **Lane-depth reconciliation (slotting)**: `POST /api/slotting/lanes/reconcile` (+ a ShedLock off-peak sweep) compares slotting's lane-depth ledger (active putaway assignments in multi-deep lanes) against inventory occupancy (reusing the occupied-locations projection), reports per-lane discrepancies {blockId, laneKey, locationId, expectedDepth, actualDepth} and corrects ghost assignments (closes them to RECONCILED); best-effort. Chore: stopped tracking the compiled `equipment-emulator` binary (now gitignored). New tests: flow `FlowMoveChainServiceTest` (cross-system + destFamily) + `TwinTelemetryServiceTest` (asrs-cranes), order-management `RouteDispatchEntityTest`, slotting `LaneReconciliationServiceTest`, emulator `asrs_test.go`. Earlier this cycle (execution-layer follow-ups): three small refinements complete the move-execution slice. (1) **Multi-leg moves (flow)** — `POST /api/flow/moves` now picks the transport by the locations' storage block: same-system → a single RELOCATE / BIN_RELOCATE (unchanged), cross-system → a RETRIEVE→CONVEY→STORE chain routed via the existing routing machinery, tracked per leg in a new `flow_move_chain` table (`V19`, idempotent per leg); the final STORE completion books the HU's destination + the `HandlingUnitMoved` audit fires (optional `destFamily`/`crossSystem` flags; induction dig-out untouched). (2) **Pick location on the order line** — allocation returns the primary allocated pick `pickLocationId` per line, order-management persists it on the order line (`pick_location_id`, `V10`), and `GET /api/orders/pick-tasks` returns the real `locationId` (fallback line pick location → latest PICK txn → null), so the guided Picking screen shows a real location code instead of blank. (3) **Metre-exact twin motion (ui)** — the hardware twin now interpolates AMR robot positions per frame (frame-rate-independent lerp toward the latest poll, heading eased to face travel) so robots glide continuously instead of snapping each ~2 s poll (totes were already smooth; cranes/ASRS are static rack geometry, AutoStore ports stationary). Earlier this cycle (execution layer): three features landed the move-execution slice — physical move dispatch (flow `POST /api/flow/moves` single-leg RELOCATE; slotting `FlowClient` + three dispatch endpoints, `V7` `device_task_id`; inventory `GET /handling-units/{id}/contents` + the dormant `StockMoved`/`PutawayCompleted` projection proven for loose stock); the picking execution UI (order-management pick queue + per-line confirm/short, the guided RF-style Picking screen at `/picking`); and the twin AMR-fleet + AutoStore live views (`/api/flow/twin/amr-fleet`, `/api/flow/twin/autostore`). Earlier this cycle (AI assistant): a new `assistant` microservice (Java, 8096) runs an Anthropic Claude Messages-API tool-use agentic loop over read-only WCS endpoints (search_orders / get_stock_by_sku / get_inventory_dashboard / get_hu_trace / get_transport_tasks / get_stock_blocking / get_dashboard), each forwarding the caller's identity + warehouse scope so RBAC applies and the model answers only from tool results; the Anthropic key + model + enabled flag live in master-data `system_configuration` (write-only on the public `/api/master-data/ai-assistant`, read by the assistant from a network-only `/internal/ai-assistant`); default model claude-haiku-4-5, claude-opus-4-8 selectable; a global floating chat widget (shown when enabled) + a Settings → AI Assistant admin tab; requires an admin-supplied Anthropic API key, an external billed dependency, and is disabled until set. Earlier this cycle, dashboards & alerting tail: real SLA metrics on Outbound (`/api/orders/reports/sla`: on-time-to-cutoff + order-cycle-time median), real inbound receive-errors (replacing the hardcoded 0), true windowed ABC risers/fallers (new `sku_pick_daily` daily tallies → exact trailing-14d-vs-90d, no more EWMA proxy), dock-to-stock timing on Inbound (`handling_unit.stored_at` → received→stored median + putaway backlog age), an alert-system-health screen (`/api/notification/alerts/health`, ISA-18.2 alarm-system measurement) and a full-screen andon board; turning demo mode ON now also seeds dashboard sample data across services and OFF tears it down; fixed a React #310 rendered-more-hooks crash on the post-login dashboards landing + topology EquipmentMesh. Earlier this cycle: the landing situation dashboard + five menu dashboards (inbound/outbound/replenishment/stock/ABC) over new read-only per-service aggregation endpoints, the notification service became functional (threshold evaluator + email/webhook alert delivery), and admin alert-threshold settings; physical moves now emit an append-only `HandlingUnitMoved` audit event to txlog — the system-of-record trail for every STORE/RETRIEVE/RELOCATE + AutoStore BIN_* completion, the audit foundation for the slotting move-dispatch work; self-service password change at login rescues "not fully set up" accounts; per-screen off/read/write access with gateway enforcement via ScreenWriteCatalog; database console governed by admin-database screen access and grantable to non-admin roles; multilanguage UI ships in 5 languages out of the box)_

Live status of the build against the roadmap in [`build.md` §15](../build.md). For what
the implemented parts actually do, see [`AS-BUILT.md`](./AS-BUILT.md).

**Legend:** ✅ functional · 🟡 partial · 🟦 scaffold (health/info only) · ⬜ not started

> ⚠️ Code is authored without a local JVM/Gradle, so it is not compiled here. **GitHub
> Actions CI** (`.github/workflows/ci.yml`) is now the verification gate: it runs
> `./gradlew build` (Testcontainers tests on Docker), builds the Go adapters + UI, and
> validates the OpenAPI specs. Watch the first run for compile errors that couldn't be
> caught locally.

---

## 1. Component status

| Component | Lang | Port | Status | Notes |
|---|---|---|---|---|
| gateway | Java | 8080 | ✅ | Routes `/api/<service>/**`; JWT validation (toggleable) + forwards/strips X-Auth-* identity; enforces warehouse scope and **read-vs-write screen access** (non-admin writes to a screen-owned path — `ScreenWriteCatalog`: master-data CRUD, counting, slotting, topology, warehouse/screen-access — rejected 403 unless the user's effective level is WRITE; override map fetched from iam `/internal/screen-access`, cached; reads + unmapped writes + IAM-down all fail-open; admins bypass). |
| master-data | Java | 8081 | ✅ | Catalog CRUD + outbound config: shippers, fulfillment config (pick types, cubing mode, batch config); dispatch reference data: shipping-service + route catalogs, label templates (+ ZPL/PDF render). **Host SKU sync** (`POST /skus/sync`): upsert (not full-catalog replace) — SKUs absent from the batch are left untouched; within each synced SKU the host is authoritative and its nested UoMs/barcodes fully replace what is stored. Whole batch in one transaction. **System config** flags in `system_configuration`: demo mode (`/api/master-data/demo`; disable = full reset via bulk DELETEs — whole SKU catalog + demo shippers/HU type, cubing default-shipper FK unset first; UI clears in two ordered phases (transport producers flow/gtp/counting/orders FIRST, then inventory + the txlog journal) so an in-flight transport callback cannot re-book a handling unit at an operational location as the registry is wiped; surfaces failed clears) and **hardware emulator** (`HARDWARE_EMULATOR_ENABLED`, `/api/master-data/emulator` get/enable/disable, ADMIN-gated, **OFF by default**); **stock rules** (`/api/master-data/stock-rules`, ADMIN-gated writes, **single-SKU-per-compartment ON by default** — enforced by GTP decanting); polled by the `equipment-emulator` service — when ON, flow-orchestrator routes device tasks there instead of the real adapters. **Admin database console** (`/api/master-data/admin/db`, ADMIN-gated): `GET /schemas` (non-system schemas + tables + columns) and `POST /query` running exactly one SELECT — validator (masked literals/comments, multi-statement + write/DDL keyword rejection incl. CTE-smuggled writes) plus a READ ONLY rolled-back transaction, 10 s statement timeout, row cap 200/1000 with `truncated` flag; audit at INFO without SQL text. **Operational locations** (`GET /locations/operational?warehouseId=&kind=EQUIPMENT|WORKPLACE|UNKNOWN&name=`): lazily-created per-warehouse locations so HUs always book to a real place (EQUIPMENT → `CONVEYOR_SEGMENT`/`TRANSPORT`, WORKPLACE → `STATION`/`STAGING`, UNKNOWN → `FREE_SPACE`/`QUARANTINE`); idempotent under concurrency via the `(warehouse_id, code)` unique constraint + retry-fetch (`OperationalLocationTest`). **SKU card** (`GET /skus/{id}/card?warehouseId=`): one-call read of SKU identity + base-UoM item dimensions/weight + the warehouse profile's metadata, for operator screens (GTP tote panel). **Dashboard alert thresholds** (`GET` + admin `PUT /api/master-data/alert-thresholds`): JSON in `system_configuration` key `DASHBOARD_ALERT_THRESHOLDS` (defaults `scanNoReadPct=1.0`, `receiveErrorsDay=5`, `asrsUtilisationPct=85`, `blockedLinesPct=5`, `putawayBacklogAgeMin=120`, `replenishmentOldestAgeMin=120`); edited in Settings → Alerts and read by the notification evaluator + the dashboard hero state colours. **AI assistant config** (same `system_configuration` table, keys `AI_ASSISTANT_API_KEY/MODEL/ENABLED`): `GET /api/master-data/ai-assistant` returns `{enabled, configured, model}` only (never the key); admin `PUT` sets enabled/model/apiKey (**write-only** — omit `apiKey` to keep the existing key); the actual key is served to the assistant service over a **network-only** `GET /internal/ai-assistant` (never exposed through the gateway). **Hierarchical Areas** (`/api/master-data/areas`, Master Data Areas epic): a first-class **Area** entity (table `area`, `V19`) that nests inside other areas via `parent_area_id`, and a new optional `location.area_id`. `GET /areas?warehouseId=&parentAreaId=` (top-level or children), `POST`/`GET`/`PUT`/`DELETE` (archive), with **cycle** and **cross-warehouse parent** guards. `GET /areas/{id}/location-ids?recursive=true` walks the area subtree with a recursive CTE and returns every location UUID inside it (the scoping primitive for area-driven counting + replenishment claim-next); `GET /areas/resolve?warehouseId=&ref=` resolves an area by reference for the process-designer `area` verify kind (200 `found:false` on a miss). Operators get their `areaId` from an in-process picker/scan (no persistent operator → area binding). Frontend: an **Areas** tab + `AreaDialog` (nested areas, the parent select excludes self + descendants) and an **Area** select on locations. OpenAPI updated. |
| inventory | Java | 8082 | ✅ | Stock projection + SKU- and location-scoped availability/reservations. Per-location occupancy (`POST /api/inventory/locations/occupied`): which of a set of locations physically hold any stock row or handling unit (used by slotting put-away). **HU location registry** (`PUT /api/inventory/handling-units/{id}/location`): flow books (or clears) a handling unit's current storage location through the transport lifecycle so the registry stays live; consumed by ADR-0009 dig-out occupancy checks (`HandlingUnitLocationTest`). **Null bookings now go to the warehouse's UNKNOWN operational location** (HU + riding stock rows; resolved via a per-warehouse-cached `MasterDataClient`, 503 when master-data is unreachable; the old null-booking 500 is gone). Stock at UNKNOWN stays visible in the overview but is excluded from availability/ATP and can never be reserved (`UnknownLocationAllocationTest`). **Reporting aggregates**: `GET /api/inventory/reports/stock-by-sku` (available/allocated/unavailable per SKU; UNKNOWN-location stock counted unavailable) + `GET /api/inventory/reports/storage-density?days=90` (per-block daily occupied/total cells + pct from `storage_density_snapshot`; daily ShedLock-guarded sweep + on-demand snapshot for today).  **Stock follows the HU: bucket merge on collision at location bookings; HU-bound projection events book to the HU's current location, not the event's stale one.** **Dashboard aggregate** (read-only, warehouse-scoped, best-effort): `GET /api/inventory/reports/dashboard` ({husReceivedToday, huCount, skuCountWithStock, utilisationPct, asrsUtilisationPct, putawayBacklog (now with oldest-age), dockToStock:{medianMin,samples}}) for the inbound/stock dashboards. **Dock-to-stock timing**: new nullable `handling_unit.stored_at` column (`V7`), stamped on the HU's first booking into a storage slot; dockToStock = received→stored median today. Surfaced on the Inbound dashboard. **HU contents** (`GET /api/inventory/handling-units/{id}/contents`): the stock rows riding an HU (used by the move-dispatch chain). The previously-dormant **`StockMoved`/`PutawayCompleted` projection is now proven for loose (non-HU) stock** by a Testcontainers test; the HU-follows-the-HU vs explicit-from→to-move boundary is documented (HU-bound moves let the registry booking carry the location, loose stock moves apply via the projection). **Fulfilment integrity (RF epic)**: the Picked handler now **partial-consumes the named order-line reservation** (reduce `Reservation.qty`, mark `CONSUMED` when fully drawn down; `sumHeld` already excludes CONSUMED) in the same idempotent (`processed_event`) apply path as the stock decrement, so available-to-promise (`onHand − reserved`) is not double-counted by a pick (a null/missing/already-consumed reservation is a no-op, so legacy events / replays just decrement). The `StockAdjusted` (Adjust) payload gained an optional `reason` field for audit. The non-negative stock guard (DB check + projection underflow throw) is unchanged; no migration. |
| order-management | Java | 8084 | ✅ | Orders of all types (INBOUND/OUTBOUND/COUNT/ADJUSTMENT), lifecycle, release mgmt, dispatch service/route + ship-to + label-template (validated against master-data), line stock transactions via a local outbox → txlog (audit: actor required); delegates allocation. **Demo quick-seed** `POST /api/orders/demo/seed {type INBOUND/OUTBOUND, count?:10}` (demo-mode-only; outbound seeds are small single-line orders that fit one shipper; drives the demo-only "Add 10 Orders" buttons on inbound/outbound). **Order-flow report** `GET /api/orders/reports/flow?direction=INBOUND|OUTBOUND&days=90`: expected/active/started headline figures + zero-filled per-day (received/started/completed) and hour-of-day histograms; lifecycle mapping documented in `order-management.yaml`. **Short allocate and release** (`POST /api/orders/{id}/release-short`, `ORDER_RELEASE` = SUPERVISOR/ADMIN, actor logged): a NOT_FULFILLABLE order re-allocates allow-short and releases as `PARTIALLY_ALLOCATED` with per-line `allocatedQty` + `SHORT` statuses; 409 if not short or nothing available. Ship now accepts `PARTIALLY_ALLOCATED` and stages an order-level **OrderShipped** outbox event (per-line orderedQty/shippedQty/shortQty + shortShipped) for the host confirmation feed (`V9`: nullable `order_outbox.line_txn_id`). **Dashboard aggregates** (read-only, warehouse-scoped, best-effort, no new entity, derived from orders + routes): `GET /api/orders/reports/dashboard` ({inbound, outbound, perHour}) for the inbound/outbound situation tiles (inbound now carries a real `receiveErrorsToday` — INBOUND lines with a receipt discrepancy today: over-receipt, or under-receipt on a closed-short line — replacing the previous hardcoded 0), and `GET /api/orders/reports/dispatch` ({nextDeparture, routes[]}) for the dispatch tile. **Persisted dispatch waves (residual)**: a new `route_dispatch` entity (`V11`, one row per (warehouse, route, cut-off): status OPEN→LOADING→DEPARTED, `opened_at`/`first_loaded_at`/`departed_at`, `orders_assigned`/`orders_ready`) is lazily upserted when an outbound order is first assigned to a (route, cut-off) and advanced best-effort **in the caller's transaction** as its orders are assigned / released / shipped (`RouteDispatchService.onOrderChanged`; counts + status recomputed from the orders each call, so it self-corrects and never regresses; a bookkeeping failure is swallowed and never breaks the order lifecycle). `GET /api/orders/reports/dispatch` is now backed by this entity (real status + accurate `departed_at`, same `{nextDeparture, routes[]}` shape), so the dispatch tile is first-class rather than purely derived. **SLA report** `GET /api/orders/reports/sla?warehouseId&days` ({onTimeToCutoffPctToday, orderCycleTimeMedianMinToday, perDay:[{day,onTimePct,cycleMedianMin}]}): on-time now uses the order's **route-dispatch wave `departed_at`** vs cut-off when the wave has departed (the real departure instant), falling back to the order's own ship time otherwise; cycle time = created→shipped median. Surfaced as heroes on the Outbound dashboard. (`RouteDispatchEntityTest` covers the wave lifecycle + dispatch/SLA backing.) **Picking execution** (execution layer): `GET /api/orders/pick-tasks?warehouseId` (the operator pick queue — released/allocated outbound lines that still need picking, ordered for a pick walk) + `POST /api/orders/pick-tasks/{lineId}/confirm {pickedQty, short?}` (posts a `Picked` line transaction via the existing outbox→txlog, decrements stock, advances `pickedQty`, marks `SHORT`). RBAC: `ORDER_VIEW` read / `ORDER_POST_TRANSACTION` write. **Pick location on the line**: allocation returns the primary allocated `pickLocationId` per line, persisted on the order line (`pick_location_id`, `V10`), so `pick-tasks` returns the real `locationId` (fallback: line pick location → latest PICK txn → null) and the guided Picking screen shows a real location code. **Correct pick fulfilment (RF epic)**: confirm now reads the pickable qty at the face (`GET /api/inventory/availability?warehouseId=&skuId=&locationId=` via a new inventory client, base url `OPENWCS_INVENTORY_BASE_URL`, default :8082) and **clamps the booked qty to what is on hand**, marking the line SHORT when it clamps (best-effort: a read failure falls back to the requested qty; the inventory projection's non-negative check is the hard backstop), so a pick can never drive stock negative; it **consumes the order line's reservation** (the Picked event now carries `reservationId`, consumed by the inventory projection so ATP is not double-counted); and it **stamps the operator (`actor`/`X-Auth-User`), the driving process instance (`processInstanceId` body field or `X-Process-Instance` header) and the order type** on the PICK transaction (`process_instance_id` + `order_type` columns, migration `V12`) and on the Picked event payload (`reservationId`, `orderType`, `processInstanceId`, `actor` alongside the existing fields). (`PickTaskTest` covers the available-at-location clamp, reservation id on the event, and the stamped audit fields.) |
| allocation | Java | 8091 | ✅ | Pick-location allocation (UoM breakdown), cubing (APP multi-size largest-first / 1:1) with per-line carton traceability + per-carton dispatch labels (host barcode per shipper), batch picking. **Allow-short mode** (`allowShort` on `POST /api/allocation/orders`): a short order keeps what it could reserve, marks per-line shortfalls (`SHORT`, zero-stock lines fully short), cubes only the allocated quantities and returns `FULFILLABLE_SHORT` (`V5`); strict mode unchanged; batch picking accepts both reservation-holding statuses. **Dashboard aggregate** (read-only, warehouse-scoped, best-effort): `GET /api/allocation/reports/stock-blocking` ({blockedLines, distinctSkusShort, recoverableLines, zeroStockLines, openOutboundLines}) for the stock-blocking situation tile + replenishment dashboard. |
| slotting | Java | 8093 | ✅ | Put-away assignment for automated rack/GTP blocks (weighted scorer: velocity-to-exit · same-SKU lane consolidation · aisle redundancy cap/floor · fill balance; exit distance = `location.distance_to_exit` when set, else derived from the cell coordinate (posX−1)+(posY−1)+(posZ−1) with the port assumed at position 1 ground level, and the guided rack builder stamps it on generation; soft single-SKU-per-lane (mixing penalty, outweighable by balance) + hard lane-capacity/max-aisle constraints; direct-to-pick), manual pick-face slotting + min/max replenishment (opportunistic off-peak top-off), off-peak re-slotting. Put-away candidates are filtered against live inventory occupancy (`POST /api/inventory/locations/occupied`) so physically-occupied slots are excluded before scoring (best-effort if inventory is down). **Profile-less SKUs fall back to the warehouse's only automated storage block** (SHUTTLE_ASRS/CRANE_ASRS/AUTOSTORE/AMR_GTP; ambiguous = several automated blocks still 400). Goods-in `assignPutaway` BPMN delegate. ADR 0003. **Relocation plan** (`POST /api/slotting/relocation-plan {warehouseId, locationId}`): for ADR-0009 multi-deep shuttle channels, plans the dig-out sequence before a blocked retrieve — ordered `{huId, fromLocationId, toLocationId}` steps for blockers occupying shallower cells (target: same `cellY` hard constraint, same aisle preferred; empty when the channel is clear); occupancy from the HU location registry, channel siblings from master-data cell coordinates (`RelocationPlanApiTest`). **Move dispatch ✅** (execution layer): a `FlowClient` + three dispatch endpoints turn PLANS into real moves via flow `POST /api/flow/moves`: `POST /api/slotting/reslot/{id}/dispatch` (re-slot RELOCATE), `POST /api/slotting/replenishment/{taskId}/dispatch` (source resolved best-effort / FEFO-approx → pick face), `POST /api/slotting/decant/putaway {warehouseId,huId,skuId,qty}` (runs the put-away scorer then dispatches the STORE). Plans flip to a new **DISPATCHED** status (migration `V7` adds `device_task_id` to `reslot_recommendation` + `replenishment_task`); best-effort — a flow failure leaves the plan un-dispatched. Compose: slotting gained `OPENWCS_FLOW_ORCHESTRATOR_BASE_URL`.  **Lane-depth reconciliation (residual)** (`POST /api/slotting/lanes/reconcile?warehouseId=&blockId?=`, OPERATOR/ADMIN + warehouse scope; plus a ShedLock off-peak sweep `LaneReconciliationScheduler`, cron `openwcs.slotting.offpeak-cron`, default 02:00): for every multi-deep lane (`laneDepth>1`) it compares slotting's lane-depth ledger (PLANNED/DISPATCHED putaway assignments per cell, grouped into lanes by aisle+level+column) against inventory's actual occupancy (reusing the `POST /api/inventory/locations/occupied` projection) and reports per-lane discrepancies `{blockId, laneKey, locationId(face cell), expectedDepth, actualDepth}`; where the ledger holds an assignment for a cell inventory reports empty (a ghost), it corrects to truth by closing the assignment (status RECONCILED). Best-effort: an inventory hiccup skips the lane (degrades to an empty pass). (`LaneReconciliationServiceTest`.)  **Assignment lifecycle**: a fresh putaway SUPERSEDEs the HU's open assignments (one live plan per tote); flow confirms the completed ASRS STORE via `POST /api/slotting/putaway/stored` → status STORED (open assignments are planned occupancy); the hard aisle-share cap is skipped in single-aisle blocks (no alternative aisle exists — it previously rejected every SKU's second putaway there). **Dashboard aggregates** (read-only, warehouse-scoped, best-effort): `GET /api/slotting/replenishment/dashboard` ({urgent, openTasks, oldestAgeMin, projectedStockouts}) for the replenishment dashboard, and `GET /api/slotting/velocity/abc` ({a, b, c, pareto, top, bottom, risers, fallers}) for the ABC-movers dashboard — risers/fallers are now **true windowed**: slotting persists per-SKU daily pick tallies (new table `sku_pick_daily`, `V6`, upserted idempotently by the velocity consumer) and computes exact trailing-14d-vs-90d pick rates (the old EWMA short-vs-long proxy is gone). **Per-operator replenishment reservation** (area-driven, Master Data Areas epic): `POST /api/slotting/replenishment/claim-next {warehouseId, areaId, instanceId}` atomically reserves the next `PLANNED` replenishment task whose **destination pick face** (`to_location_id`) is inside a Master Data Area subtree (resolved via master-data `GET /api/master-data/areas/{id}/location-ids?recursive=true`), **EMERGENCY first**, with Postgres `FOR UPDATE SKIP LOCKED` so two operators never claim the same task; returns `{found, taskId, skuId, fromLocationId, toLocationId, qty, uomId, priority}`. `POST /api/slotting/replenishment/release {instanceId}` frees not-yet-started reservations (idempotent). `started_at` is stamped on dispatch (immune to release/sweep); a ShedLock sweeper reclaims stale reservations (TTL `openwcs.slotting.reservation-ttl`, default 15m). Schema: `replenishment_task` gains `reserved_by`/`reserved_at`/`reserved_by_instance`/`started_at` (`V8`). |
| gtp | Java | 8094 | ✅ | Goods-to-person station execution (ADR 0006): station + STOCK/ORDER node config, open order destinations (bind order HU + demand), present a stock HU → put-to-light put-list (greedy most-needed-first; one HU serves many orders = batch), confirm puts (full/short), complete destinations. ORDER_LOCATION (conveyor) + PUT_WALL (lit rack/AMR) destination topology share one engine. **Configurable pick layout** (`V10`, `pick_layout` + `pick_slots`): ONE_TO_ONE (1 source → 1 target), ONE_TO_N (N lit pick slots, N ≥ 2), or PUT_WALL (lit cubbies, requires `mode = PUT_WALL`); surfaced on StationView / workplace / WorkCycle and set in the GTP config screen; the operator console renders the target side by layout (single carton / N slots grey-orange-green tap-to-confirm / only the needed put lights). **Pick-to-light driving** (`LightControlClient`): on present the gtp service ILLUMINATEs each put light, on confirm + close it CLEARs — best-effort (a light failure never blocks the pick), to the PTL adapter (:9098) or the equipment-emulator's PTL family when the hardware-emulator flag is on. Orthogonal operating modes (PICKING / DECANTING / STOCK_COUNT / QC / MAINTENANCE): generalised work-cycle with mode-appropriate task lines + outcomes (decant-moves, count entries + variance, PASS/FAIL/HOLD verdicts, OK/DEFECTIVE/REPAIR checks). **Station inbound queue — ADR-0007 Phase 3c-1: source of truth relocated to flow-orchestrator** (`induction_queue_entry`). GTP's `POST /stations/{id}/queue` inbound enqueue is **deprecated** (counting no longer calls it); `POST /queue/{entryId}/complete` fans out to flow `POST /api/flow/induction/entries/{id}/done` and dispatches NO transport of its own — flow's return leg owns the store-back and only slotting picks the destination (gtp's parallel store-back + `SlottingClient` removed). Legacy `station_queue_entry` code kept but marked `@Deprecated`. **Deactivate/drain** (`POST /stations/{id}/deactivate|activate`): finishes queued work, rejects new HUs while draining; **`WorkplaceView` includes `acceptingWork`** so the console restores the drain-switch state on reload. **In-transit capacity caps** (`POST /stations/{id}/capacity`): max simultaneous inbound transports split by mode class (read by flow-orchestrator's `WorkplaceClient` to meter RETRIEVE dispatch). **Topology node sync** (`POST /stations/{id}/nodes/sync`): replaces STOCK/ORDER nodes from a topology projection — matched by code (preserves bound demand/HU), carries `inboundDistanceM` (feeding conveyor distance); called by flow-orchestrator on topology projection. `V7__station_node_distance.sql` adds `inbound_distance_m` to `station_node`. **Store-back ownership**: moved entirely to flow-orchestrator's return leg (slotting-only; no gtp-side STORE) — the old parallel dispatch double-stored totes. Seams: physical lights + picking-HU retrieval (adapters/flow-orchestrator), demand auto-wire from allocation, stock→txlog, decant→slotting-putaway, count→inventory-StockAdjusted — all follow-up. **PICKING console, by layout** (frontend `GtpOpsScreen.tsx`): the active pick is one box split left/middle/right — source | take qty (`→ N →`) | target by layout: ONE_TO_ONE a single focused qty input (Enter = full, lower = short), ONE_TO_N all N slots (`SlotsView`: grey unused / orange in-use / green picked, tap to confirm, short stepper), PUT_WALL only the needed put lights (orange→green, tap to confirm); backend calls unchanged. |
| assistant | Java | 8096 | ✅ | **AI chat over warehouse data.** `POST /api/assistant/chat {warehouseId, messages[]}` → `{reply}` runs the **Anthropic Claude Messages API tool-use agentic loop** (official anthropic-java SDK) over a fixed set of **read-only** tools that are HTTP calls to existing endpoints — `search_orders`, `get_stock_by_sku`, `get_inventory_dashboard`, `get_hu_trace`, `get_transport_tasks`, `get_stock_blocking`, `get_dashboard` — each forwarding the caller's `X-Auth-User`/`X-Auth-Roles`/`X-Auth-Warehouses` so RBAC + warehouse scope apply and the model answers ONLY from tool results. `GET /api/assistant/status` → `{enabled, configured, model}`. Default model **claude-haiku-4-5** (Haiku 4.5), **claude-opus-4-8** (Opus 4.8) selectable. RBAC: authenticated users (INVENTORY_VIEW). **Key handling**: the Anthropic API key + model + enabled flag live in master-data `system_configuration` (`AI_ASSISTANT_API_KEY/MODEL/ENABLED`); the key is **write-only** on the public master-data API and the assistant reads the actual key from a **network-only** internal endpoint (`GET /internal/ai-assistant`, never gateway-exposed, mirroring iam's `/internal/screen-access`). Feature is disabled until an admin sets a key and enables it. **No DB** (stateless). **Requires an admin-supplied Anthropic API key (external dependency, billed).** Gateway route `/api/assistant/**`; compose + k8s wiring. |
| counting | Java | 8095 | ✅ | Cycle/stock counting: count tasks (LOCATION/SKU/ZONE/BLOCK scope, BLIND/VARIANCE), ABC-cadence schedule generator, capture → variance vs inventory snapshot → within-tolerance auto-approve (posts `StockAdjusted`) / out-of-tolerance recount. **Delete OPEN tasks** (`DELETE /tasks/{taskId}`): removes a task + its lines while still OPEN; 409 once active. Seams (by id): GTP STOCK_COUNT station, cycle-count BPMN; adjustment via txlog. **Count-tote routing** (emulator mode, ADR-0007 Phase 3c-1): on count-task creation, automated-storage cells (`SHUTTLE_ASRS`, `CRANE_ASRS`, `AUTOSTORE`, `AMR_GTP`) are automatically routed to an active GTP STOCK_COUNT station via a single `flow.requestPresentation(...)` call — flow creates a `REQUESTED` induction entry and orchestrates the RETRIEVE + CONVEY journey itself, dispatching to the correct adapter family (`ASRS`, `AUTOSTORE`, or `AMR`). The cap is metered by flow at RETRIEVE dispatch; `REQUESTED` is uncapped so routing always succeeds at request time; best-effort (never breaks task creation). **Demo quick-seed** `POST /api/counting/demo/seed {count?:1}` (demo-mode-only; drives the demo-only "Add count task" button, one task per click). **At-station blind count** (`POST /tasks/{taskId}/lines/{lineId}/station-count {countedQty}`): blind state machine on `count_line` (`station_count_state`: PENDING → RECOUNT → ACCEPTED | ADJUSTED); two agreeing counts that differ from expected post a `StockAdjusted` (delta = counted − expected, reason `COUNTING`, actor from `X-Auth-User` / `?operator=`, fallback `"system"`); all-terminal lines → task `RECONCILED`. Schema: `V4__count_line_station_count.sql` + `V5__count_line_hu.sql` (`hu_id uuid` — tote snapshotted from inventory at task generation, carried on `StockAdjusted` events to target the correct tote bucket; null for bin stock). **Per-operator line reservation** (area-driven, Master Data Areas epic): `POST /api/counting/lines/claim-next {warehouseId, areaId, instanceId}` atomically reserves the next PENDING count line whose location is inside a Master Data Area subtree (resolved via master-data `GET /api/master-data/areas/{id}/location-ids?recursive=true`), with Postgres `FOR UPDATE SKIP LOCKED` so two operators never claim the same line; returns `{found, countTaskId, lineId, locationId, skuId, expectedQty, uomCode, countType}`. `POST /api/counting/lines/release {instanceId}` frees not-yet-started reservations (idempotent). The first recorded count stamps `started_at` (immune to release/sweep); a ShedLock sweeper reclaims stale reservations (TTL `openwcs.counting.reservation-ttl`, default 15m). Schema: `count_line` gains `reserved_by`/`reserved_at`/`reserved_by_instance`/`started_at` (`V6`). |
| txlog | Java | 8086 | ✅ | Append-only events + outbox + relay. |
| process-engine | Java | 8083 | 🟡 | Embedded Flowable BPMN: deploy definitions, start/inspect instances; service-task delegates originate WCS work (sample goods-in dispatches a device task). Process designer UI pending. |
| flow-orchestrator | Java | 8085 | 🟡 | Device-task lifecycle + **vendor-neutral conveyor routing** (topology graph w/ per-node hardware address, HU route plans, shortest-path next-hop, **loop capacity HOLD/OVERFLOW**, **topology learning** from observed scans). **Automation-topology placement** (levels, placed equipment w/ envelope + conveyor path/sections, connections, function points; `GET`/`PUT /api/flow/automation/topology`) drives the 3D/2D editor in `ui` and the routing-graph projection; placed **GTP workstations** carry a `station_id` link to their gtp_station. The projection **auto-infers connections from geometry** (equipment nodes within ~1.5 m are linked — no hand-drawn connections needed) and honours **explicit node-level connections** (`from_path_index`/`to_path_index` on `equipment_connection`, V18: stitches exactly the chosen path points at any distance and in BOTH directions (a connection links two systems; the direction of travel comes from each conveyor's own section edges), legacy connections fall back to exit/entry resolution; one dedup set across section edges + explicit + auto-inferred, so overlaps never project duplicate edges); function points can be placed **anywhere along a conveyor run** (split mid-section into named PLC node codes), and a divert/infeed stub that lands on another conveyor **merges onto it** automatically. GTP workstation role-interactions (STOCK/ORDER/DECANT, set in Properties) stay explicit. **Topology → GTP node projection**: on `POST .../topology/project`, each GTP workstation's STOCK/ORDER conveyor interactions are projected into the bound `gtp_station`'s nodes (`GtpClient.syncStationNodes`; best-effort — never aborts the routing projection). The 2D plan grid defaults to 1 m. **Flow-owned induction queue** (ADR-0007 Phase 3c-1): `POST /api/flow/induction/requests` (presentation request; flow orchestrates RETRIEVE + CONVEY, cap metered at RETRIEVE dispatch; `REQUESTED` backlog uncapped), `GET /api/flow/induction/queue?workplaceId=` (full `{REQUESTED,IN_TRANSIT,QUEUED}` pipeline, `arrival_seq` ordered), `POST /api/flow/induction/entries/{id}/done`. **Per-HU transport trace** `GET /api/flow/hu-trace?huId=`: append-only timeline (REQUESTED → RETRIEVED → INDUCTED → [RECIRCULATED/DIVERTED …] → ARRIVED → QUEUED → DONE); **Phase 3c-2**: conveyor decision points reported by the emulator (sorter `RECIRCULATED`/`DIVERTED`) are written to the trace before ARRIVED so the timeline explains arrival-order divergence (ADR-0007 R4). Schema: `induction_queue_entry` + `hu_transport_trace` (`V11__induction_queue_and_hu_trace.sql`). **Phase 3d ✅ (ADR-0008):** on each CONVEY dispatch (induction-outbound + return-to-storage leg) flow resolves the transport's entry/destination nodes via `TransportNodeResolver` and calls `assignRoute(huCode, [dest])`, putting `entryNode` in the task payload; the emulator live-walks when `entryNode` is present. `decide()` appends `SCANNED` trace rows (point = node code, decision = routed-to / held / completed) so the HU trace is a live material-flow timeline (`LiveScanConveyanceTest`). **Live-twin tote paths** (`GET /api/flow/twin/tote-paths?warehouseId=`, DEVICE_VIEW; `TwinPathService`): the "visu master" read model for the Hardware visualisation page — per in-transit HU, its actual traversed-node polyline in world XZ metres (node positions from `conveyor_node`, ordered by the running CONVEY leg's SCANNED trace, with the routed-to lead node) plus the server clock, so the 3D scene plays a backend-resolved path and never reconstructs motion by projecting scan positions onto drawn belts (which was ambiguous at diverts and flung totes to a conveyor's start). **Return-to-storage transport — slotting-only store-back:** workplace-done asks slotting (`POST /api/slotting/putaway`) for the destination; ONLY slotting slots a tote, the source slot is never a fallback. Answered → `storage_location_id` stamped, return CONVEY routed, arrival STOREs into the slotting-chosen location. Errored/no answer → the return CONVEY still goes out with NO destination + NO route plan (tote stays on the conveyor, entry flagged `awaiting_slot`, `V15`); a ShedLock-guarded sweep (`InductionSlotSweeper`, ~30s, `V16__shedlock.sql`) retries slotting and assigns destination + route mid-journey (per-scan routing; `SLOT_ASSIGNED` trace row) or fires the STORE directly when the plan-less CONVEY already completed. **HU bookings to operational locations**: CONVEY dispatch books the HU to the entry conveyor's operational location (master-data `GET /api/master-data/locations/operational?kind=EQUIPMENT&name=`, name = node code before `#`); QUEUED books the workplace's (`kind=WORKPLACE`, name = gtp station code via `WorkplaceClient.code`); STORE completion books the final slot; unresolvable → null/UNKNOWN, always best-effort. **ADR-0009 dig-out chain:** `dispatchRetrieve` calls `POST /api/slotting/relocation-plan`; when blockers exist, dispatches `RELOCATE` (or `BIN_RELOCATE` for AutoStore) for the front-most step, stamps `relocate_task_id` on the induction entry; callback books the blocker's new inventory location, writes a `RELOCATED` HU-trace row, then re-plans until channel clear → RETRIEVE goes out — stateless and self-healing. **Reporting aggregates** (`GET /api/flow/reports/{scan-quality,traffic,storage-movements,device-movements,transit-times}?warehouseId&days`, default 90 / cap 180 days, DEVICE_VIEW): `scan_stat` + `edge_traffic` daily counters (`V17__reporting_counters.sql`) bumped inside `decide()` by one atomic `INSERT … ON CONFLICT` upsert per scan/ROUTE answer (scans / no-reads / unknowns per scan point; traffic per directed edge incl. divert-default routes; a counter failure WARNs and never breaks the scan answer); storage-movements = completed STORE/RETRIEVE (+ AutoStore BIN_*) per payload `locationId` per day, device-movements = completed/failed device tasks per equipment per day (label: placed-equipment code → equipment id → family), transit-times = INDUCTED→ARRIVED per induction entry aggregated per day as count + p50/p95 ms (percentiles in Java); history accumulates from deployment day, forecasting happens in the UI; demo clear purges both counter tables. **Routing fast path (hard real-time, 300 scans/s @ ~10 ms budget)**: per-warehouse in-memory graph snapshot (`RoutingGraphCache`, immutable; nodes/adjacency/loop config) + reverse-Dijkstra next-hop tables memoised per target replace the per-scan full-edge fetch + Dijkstra; warm sync work = 1 indexed route-plan read (NOT cached: replica-shared dynamic state) + 1 route-position UPDATE (kept sync: loop occupancy + plan progression are read from it) + loop lock/count only on loop entry; counters + `SCANNED` trace move to a bounded in-process FIFO queue with one worker thread (`ScanSideEffects`; WARN-and-drop on failure); invalidation = topology PUT + routing projection (evict after commit) + defensive 60 s TTL. **`GET /api/flow/reports/decision-latency`**: {count, p50Ms, p95Ms, p99Ms, maxMs} over the last 4096 decisions, per instance (in-memory ring buffer); decisions over 10 ms WARN with phase breakdown. **Physical-move audit (`HandlingUnitMoved`)**: every terminal STORE/RETRIEVE/RELOCATE (+ AutoStore BIN_* variants) device task appends an append-only `HandlingUnitMoved` event to the txlog system-of-record (stream `hu-<huId>`; payload warehouse/hu/command/family/equipment/from+to-location/status/deviceTaskId; actor carried through) — the durable, replayable move audit complementing the flow-local HU trace. CONVEY transport is excluded (no stock-location change; already traced). Best-effort via a new `HttpTxLogClient` (short timeouts, WARN-and-continue: a txlog hiccup never fails a device-task callback). **Audit-only**: the inventory stock projection deliberately does not consume it (HU location stays maintained by the registry booking), so there is no double-apply. Sniffer capture front-end + BPMN origination pending. **Dashboard aggregate** (read-only, warehouse-scoped, best-effort, DEVICE_VIEW): `GET /api/flow/reports/automation-summary` ({scanNoReadPctToday, equipmentAvailabilityPct, equipmentTotal, equipmentInFault}) for the automation situation tile (and the notification alert evaluator). **Physical move dispatch** (`POST /api/flow/moves {warehouseId,huId,huCode,fromLocationId,toLocationId,family,reason}`, DEVICE_OPERATE): picks the transport by the two locations' storage block — same-system → a single RELOCATE (AutoStore → BIN_RELOCATE), cross-system → a RETRIEVE→CONVEY→STORE chain routed via the existing routing machinery and tracked leg-by-leg in `flow_move_chain` (`V19`, idempotent per leg). The terminal completion (the RELOCATE, or the final STORE for a chain) books the HU's new location in inventory and the existing `HandlingUnitMoved` txlog audit fires. **destFamily resolution (residual)**: for the cross-system chain the STORE leg's device family is now resolved from the **destination** location's storage block (`MasterDataClient.blockId` + `blockStorageType` → `deviceFamilyOf`: SHUTTLE/CRANE_ASRS → ASRS, AUTOSTORE → AUTOSTORE, AMR_GTP → AMR) rather than defaulting to the source family, so a cross-system move into an AutoStore destination correctly issues BIN_STORE; an explicit `destFamily` request flag still wins, and it falls back to the source family when the destination has no block / can't be resolved. Optional `destFamily`/`crossSystem` request flags; induction dig-out untouched. Stock follows the HU, so no `StockMoved` is emitted for HU-bound moves (avoids double-apply). (`FlowMoveChainServiceTest` covers the cross-system path + destFamily resolution.) **Twin live views** (DEVICE_VIEW, best-effort empty when the emulator is off): `GET /api/flow/twin/amr-fleet?warehouseId=`, `GET /api/flow/twin/autostore?warehouseId=` + `GET /api/flow/twin/asrs-cranes?warehouseId=` are twin read models (`TwinTelemetryService`) that map `equipment-emulator` AMR/AutoStore/ASRS-crane telemetry to world coordinates for the hardware-twin scene (each crane: world x/z aisle floor + y lift height, IDLE/MOVING/FAULTED, carried HU; positions fall back to the placed ASRS equipment when the emulator omits them). (`TwinTelemetryServiceTest` covers asrs-cranes.) |
| iam | Java | 8087 | ✅ | Authorization model: users → roles → coded permissions; seeded roles; effective-permission resolution; **per-user warehouse access** (allowed warehouses + one default; admin-only writes; the gateway enforces warehouse scope). **Per-screen access levels** (`/api/iam/screen-access`, tables `screen_access` + `_role`/`_user` with `access_level` column, `V6`): off/read/write per role + read/write per allowed user; gateway enforces WRITE for non-admin mutating calls to screen-owned paths; `GET /internal/screen-access` (network-only) feeds the gateway cache. **Database console access-required route**: the gateway checks `admin-database` screen access (≥ READ) on all methods to `/api/master-data/admin/db/**` — grantable to non-admin roles/users via Access control. **Self-service password change** (`POST /api/iam/change-password`, public — the one unauthenticated `/api/**` route): proves identity with the current password and sets a permanent new one via the `openwcs-iam` service-account client; rescues temporary/forced-password accounts. **Per-user UI language** (`AppUser.language`, `V5`): `GET`/`PUT /api/iam/me/language` — drives the frontend i18n (unknown codes coerced to `en`). (Keycloak does auth.) |
| notification | Java | 8088 | ✅ | **Operator alerting** (was an empty scaffold). `alert_event` store (`V1`) + ShedLock (`V2`); a **ShedLock-guarded `@Scheduled` evaluator** (~60 s) pulls the dashboard metric endpoints (flow automation-summary, inventory dashboard, allocation stock-blocking, slotting replenishment, order dispatch) + per-warehouse thresholds (master-data `GET /alert-thresholds`) and, per warehouse, opens **WARNING/CRITICAL** alerts when a metric breaches its threshold, **dedupes** by (warehouse, area, metric), and **clears** the alert when the metric falls back under. **Delivery**: each open/clear is sent by **email** (Spring Mail / SMTP, degrades to a log line when unconfigured) and **webhook** (`ALERT_WEBHOOK_URL`). API: `GET /api/notification/alerts?warehouseId=` + `POST /api/notification/alerts/{id}/ack` (SUPERVISOR/ADMIN). **Alert-system-health** (ISA-18.2 alarm-system measurement) `GET /api/notification/alerts/health?warehouseId&days` → { activeBySeverity, perDay:[{day,opened,cleared}], chattering:[{area,metric,flaps}], stale:[{id,area,metric,ageMin}] } (drives the alert-health UI screen). **Demo clear** `POST /api/notification/demo/clear?warehouseId=` removes only `DEMO-`-marked `alert_event` rows (wired into demo-off). Gateway route `/api/notification/**`; compose + k8s env for the metric-source URLs + SMTP + webhook. OpenAPI `contracts/openapi/notification.yaml`. |
| integration-sap | Java | 8089 | 🟡 | Host gateway: per-shipper dispatch-label barcode (`POST /labels`) + route feed (`POST /routes/sync`) + SAP order/ASN intake (`POST /orders`,`/asns`) translated into the canonical Host API (materials resolved to SKUs). |
| integration-manhattan | Java | 8090 | 🟡 | Host gateway: Manhattan order/ASN intake (`POST /orders`,`/asns`) translated into the canonical Host API (items resolved to SKUs). |
| integration-host | Java | 8092 | 🟡 | Canonical vendor-neutral Host API (`/api/host/**`): orders + ASNs + **SKU sync (list of SKUs with UoMs + barcodes inline)** + inventory adjustments in; confirmations out via pull (cursor feed) **and** webhook push; idempotency keys. |
| equipment-emulator | Go | 9097 | 🟡 | Simulates all device families (conveyor/ASRS/AMR/AutoStore commands, plus a **PTL** family ILLUMINATE/CLEAR that tracks the lit lights in `GET /state` for the gtp light client); active when `HARDWARE_EMULATOR_ENABLED` is ON — flow-orchestrator routes device tasks here instead of the real adapters. Each command sleeps a realistic per-family/command duration before returning COMPLETED (result includes `durationMs`); `OPENWCS_EMULATOR_LATENCY_MS` overrides all commands (`0` = instant). **Fault injection** (`OPENWCS_EMULATOR_FAULT_RATE=N`): 1 in every N tasks returns FAILED (deterministic; result carries `fault: true`). **Loop recirculation** (`OPENWCS_EMULATOR_RECIRC_EVERY=N`): every Nth CONVEY recirculates once before diverting — arrival order visibly diverges from dispatch order (ADR-0007 R2); CONVEY result carries `recirculations` count + `decisions` list (sorter `RECIRCULATED`/`DIVERTED` points) written to the HU trace by flow (R4). **Live config** (`GET`/`POST /config`): latency, fault rate, and recirc rate are atomics changeable at runtime without restart (`{latencyOverrideMs, faultEvery, recircEvery}`), plus `asrsHandoverMs`). **ASRS shuttle/lift/handover serialisation** (`handover.go`): an ASRS (and AutoStore) moves ONE tote at a time, so consecutive tote-moving commands (STORE/RETRIEVE/RELOCATE, BIN_*) on the same device are serialised and spaced by `asrsHandoverMs` (default 1800 ms, env `OPENWCS_EMULATOR_ASRS_HANDOVER_MS`, live-tunable via `/config`) — totes leave storage staggered instead of two retrievals completing at once and entering the conveyor on the same spot (which rendered as a single tote on the twin). **Real telemetry** (`GET /state`): per-family `completed`/`failed` tallies from actual task load; snapshot echoes live config. **Twin live telemetry** (execution layer): `GET /amr/fleet` (simulated AMR robot positions / status / carried HU) + `GET /autostore/grid` (grid fill + per-port activity) + `GET /asrs/cranes` (a small fixed set of simulated storage/retrieval cranes, one per aisle, each with a world XZ + lift height y, IDLE/MOVING/FAULTED, carried HU); AMR, AutoStore and ASRS task processing updates this simulated state (an ASRS STORE/RETRIEVE/RELOCATE makes the next crane MOVING with its HU, travelling the aisle + lifting for the task duration, then idling and lowering), so the twin's AMR-fleet + AutoStore + ASRS-crane views animate against real task load (`twin_test.go`, `asrs_test.go`). **Live conveyor walk** (ADR-0008 Phase 3d-2, `walk.go`): a CONVEY task with an `entryNode` payload enters live-walk mode — fetches the routing topology from flow, then loops: `POST /api/flow/conveyor/route`, travel the replied edge at `0.5 m/s` (`speedMps` — live-tunable in `/config`), HOLD → dwell and rescan, COMPLETE → result callback; fails on NO_ROUTE/EXCEPTION; safety cap at 500 scans; `recircEvery` simulation does not apply to live walks; falls back to atomic sleep when no `entryNode`. `/config` gains `speedMps`; `walk_test.go` covers the scan loop, HOLD behaviour, and atomic fallback. **Decision-grade daily logs**: task accept/complete/fault/walk lines carry HU code, route, equipment and the why (recirculation policy, fault injection, hold reason, no-route rescans); `/config` changes log old -> new; degraded paths at WARNING with consequence. |
| adapters/conveyor | Go | 9091 | 🟡 | Health + stub loop; `POST /tasks` returns FAILED ("hardware not connected") — real-hardware seam. Emulation consolidated into `equipment-emulator`. Refusals logged at WARNING with HU code, route, equipment, reason and consequence. |
| adapters/{asrs,amr-geekplus,autostore} | Go | 9096, 9093, 9094 | 🟡 | Health + stub loop; `POST /tasks` returns FAILED ("hardware not connected") — real-hardware seam. Emulation consolidated into `equipment-emulator`. (asrs on 9096 — 9092 is Kafka's.) Refusals logged at WARNING with HU code, equipment, reason and consequence. |
| adapters/ptl | Go | 9098 | 🟡 | Pick-to-light (PTL) adapter, family `PTL`, driven by the **gtp** service (not flow). `POST /tasks` accepts `ILLUMINATE` (light id + qty + optional colour) and `CLEAR` (→ COMPLETED), any other command → FAILED; tracks the lit set in memory and exposes it at `GET /state` (+ `/healthz` `/readyz`); the `deviceLoop` stub is the real HTTP/serial PTL-controller seam. The gtp `HttpLightControlClient` targets it (or the equipment-emulator's PTL family when the hardware-emulator flag is ON), best-effort. |
| adapters/conveyor-sniffer | Go | 9095 | 🟡 | Ingests scan telegrams from defined source IPs (allowlist + decoder) → posts observations to the WCS for topology learning. Logs stream connect/close with per-session tallies, each forwarded scan (barcode + node + controller), and WARNINGs with the raw line for undecodable telegrams / lost observations. |
| ui | React/TS | 5173 dev / 443 prod | 🟡 | React/Vite SPA, now an **installable PWA** (vite-plugin-pwa: web manifest + a workbox service worker precaching the app shell so a Wi-Fi blip never white-screens a handheld; `/api` is NetworkFirst with a 4 s timeout so picking data is never served stale): **Keycloak login** + sidebar **app shell** + **dashboard** + **screen permission catalog** (role/user-gated nav & routes with a per-screen **off / read / write** level, overridable via Access control — a 3-way Off/Read/Write per role + Read/Write per allowed user; READ = view-only, write controls hidden/disabled on the write-heavy screens and writes rejected at the gateway). Built screens: inbound, outbound, counting (in-app confirm dialog for OPEN-task delete), **Picking** (Operations, `/picking`: a **scanner-first RF operator console** for rugged Android handhelds in keyboard-wedge mode — a big glove-friendly next-pick card with the location code, SKU code + name + image and qty, an **auto-refocusing capture input** that reads wedge scans (keystrokes + Enter) and matches them against the current pick's expected location code then SKU code (mismatch shows a big warning + vibrate) and auto-confirms at full qty, **Confirm** / **Short** still available manually, an advancing pick queue from `GET /api/orders/pick-tasks`, codes via `useCatalog` (no UUIDs); an **offline confirm queue** (IndexedDB, localStorage fallback) durably holds confirm POSTs that fail on network / 5xx and advances optimistically, retrying on `online` / interval / manual (4xx = permanent-failed, 409 = already-done), with an "Online / Offline, N pending" banner; operators configure the Zebra/Honeywell **DataWedge** profile for keystroke output with an Enter suffix, and "Install app" to run standalone; pick-by-light / voice are further hardware seams layered on this screen; the configurable-process mobile runtime additionally persists **every screen advance** to a durable IndexedDB step queue and advances immediately, so a refresh / device switch / RF re-login resumes on the EXACT step with data intact, and the handheld OperatorMenu has a **"Resume in-progress work"** launcher; a supervisor **RF-Users** screen (`/rf-users`, Engineering) lists active operators with their process + current workstep, with **Reassign** and a **Replay** viewer that reuses the real screen renderer), **GTP operator console** (single-active-session; **queue-driven + auto-present**: polls the station queue every 3 s and auto-presents the head QUEUED tote in PICKING mode — no manual form; **operating mode persisted per station** (localStorage — restored on reload); **per-mode accent hue** (counting orange, QC + maintenance red, picking/decanting green: mode pills + mode panel borders, via `modeAccent()` in `GtpOpsScreen.tsx` reusing the `--warning`/`--danger`/`--herbal-lime` tokens); **active-tote panel** shows a tote glyph + HU code hero, the SKU code wipth the SKU name on its own line, qty, SKU image and a metadata chip strip (base-UoM item dimensions/weight + `sku_profile` metadata via the one-call SKU card read, session-cached, never delaying the tote presentation; same identity block in the blind-count `CountPanel`); **fills the viewport when no cycle is active** (larger SKU image, centred; idle **waiting-for-totes** state also full-screen); queue surfaced as a right-side fold-out **drawer** (slim handle on right edge, slides open with backdrop) — now reads from `GET /api/flow/induction/queue?workplaceId=` and surfaces `REQUESTED` (in-storage, not yet retrieved), `IN_TRANSIT`, and `QUEUED` entries so operators see the full inbound pipeline; STOCK_COUNT mode also queue-driven — queue entries with `countTaskId`/`countLineId` trigger a **`CountPanel`** (blind: no expected qty; clears on RECOUNT, advances on ACCEPTED/ADJUSTED); falls back to "Done counting" button otherwise; **deactivate/activate** controls) + **GTP workplace config** (node locations searched by **location code**, not UUID; destination topology asked only for PICKING workplaces; **capacity configuration** for in-transit HU caps per mode class), **transport overview** (HU code column; origin/destination/next-hop resolve UUIDs to location codes via `useCatalog` or GTP station codes via `listWorkplaces`; fallback to short id; next-hop falls back to destination for ASRS/AMR/AutoStore direct-delivery tasks; **click-to-trace dialog**: click a row → detail + trace dialog with code-resolved fields; when the task has an HU id, shows the per-HU transport trace from `GET /api/flow/hu-trace?huId=`, **scoped to this transport's induction run** (rows sharing the selected task's `inductionEntryId`; falls back to the full HU timeline when no run-linked rows exist) — REQUESTED → RETRIEVED → INDUCTED → ARRIVED → QUEUED → DONE, with point, decision, timestamps; falls back to the device-task correlation trace from `GET /api/flow/device-tasks?correlationId=` when no HU id is resolvable; **scope filter** replaces the old status filter: "Open + finished today" (default working-set), "Open (active) only", "Completed", "Failed", "All recent" — client-side on a 500-task backend window), stock transactions; **hardware twin** (`/hardware-twin`, Operations: live 3D digital twin — equipment coloured idle/running/faulted from device tasks, conveyor bodies tinted directly with their live state colour (green functional / orange jam or heavy traffic / red fault — no translucent overlay; tint threaded through the editor mesh's bodyTint prop, since ConveyorPath ignores `color` for real conveyors), totes moving smoothly along the **backend-resolved conveyor path** (the flow-orchestrator "visu master" `GET /api/flow/twin/tote-paths` returns each in-transit tote's actual traversed-node polyline in world metres + the server clock; the frontend plays it with straight segments between graph-adjacent nodes — no client-side belt projection, which used to fling totes to a conveyor's start at diverts; render clock anchored to the server clock so skew can't misalign it, `hardwaretwin/motion.ts`); queued totes wait ON the station's inbound conveyor (the active/head tote at the editor's link point, held until the workplace releases it; the rest queued one slot upstream each — totes never move onto the workstation box), and two totes sharing a belt are spaced nose-to-tail (anti-stack along each tote's own heading) so simultaneously-retrieved totes do not render as one + ASRS cell positions, level selector + labels toggle; **live AMR fleet + AutoStore views** (execution layer): the scene now also renders the **AMR fleet** (robots coloured by status, carried HU shown on hover, from `GET /api/flow/twin/amr-fleet`) and **AutoStore ports** (busy/idle) plus an **AutoStore fill %** stat (from `GET /api/flow/twin/autostore`), and **live ASRS cranes** (carriage meshes gliding in their aisle at world (x, y, z), status-coloured, carried HU on hover, legend entry, from `GET /api/flow/twin/asrs-cranes`); all poll ~2 s and degrade to nothing when the emulator is absent; AMR robots **and ASRS cranes** are **interpolated per frame** (frame-rate-independent lerp toward the latest poll, heading eased to face travel) so they glide/lift continuously instead of snapping each ~2 s poll (totes were already smooth; AutoStore ports stationary)); **automation topology** (React Flow; 2D plan editor: divert direction picker (L/S/R, min 2), double-click-to-draw corners, click-to-place ASRS IN/OUT, waypoint delete, click-select workstation conveyor function-points in 3D, **divert default direction** Select on a divert's function-point dialog (None (stop)/Straight/Branch, persisted with the topology); **routing graph tab**: generated routing graph as a read-mostly table inspector incl. each divert node's projected **default exit**; **route test mode**: pick start/target in the 3D scene — runs Dijkstra over the exact directed graph, renders the path + a 0.5 m/s ghost tote, or the reachable frontier when no path exists; **node-link visibility + explicit links** (`nodeLinks.ts`, pure client mirror of the projection's adjacency): live green/amber indicators in 3D + 2D where equipment meets (linked vs "not linked" with the gap in metres), a per-node **Connections section** in Properties showing each end node's counterpart (distance + auto/explicit) with closest-first link candidates, and Unlink with honest still-auto-linked feedback; the Properties panel's explicit-connection list (`Connections (N)`) is **click-to-open**: a row opens a **connection detail/edit dialog** (inline Delete does not open it) showing both endpoints un-truncated with the exact anchored node `CODE#index`, hand-drawn vs auto-inferred-endpoint type, the gap in metres, and the status, with editable from/to path point (Auto fallback or any routable node), label and status that mutate the in-memory connection and round-trip through Save); BPMN designer (bpmn-js), slotting, master data (SKU/UoM/barcode read-only), settings (incl. a **Cubing** tab: edit cubing rules + full shipper CRUD with fill rates, and a **Hardware emulator** toggle), user management, access control, **warehouse access** (per-user allowed warehouses + default), **system info** (version, health **and logs** of every service/adapter via the gateway's `GET /api/system/services` + `…/{name}/logs?date=` + `…/log-days`; build-info stamped into each jar/adapter at build; per-service **daily log files**, 14-day retention, on a shared `openwcs-logs` volume — Java via libs:common logback, Go via a daily writer); **database console** (Administration → Database, `/admin/database`, governed by `admin-database` screen access — ADMIN by default, grantable + gateway-enforced: schema → table tree, click-to-query (`select * … limit 100`), monospace SQL editor with Cmd/Ctrl+Enter, DataTable results with row count + truncated badge + execution time, PostgreSQL errors surfaced, last query in localStorage — SELECT-only backend). A global **top-bar warehouse switcher** auto-selects the user's default on login and scopes every warehouse-related screen (no UUID entry). **Frontend i18n** (English/German/French/Spanish/Chinese/Brazilian Portuguese): a lightweight dependency-free framework (`src/i18n/`, `useT(ns)` → `t(key, english)`, glob-discovered `locales/<lang>/<namespace>.ts` so namespaces are conflict-free to translate in parallel; English is the inline fallback) plus a top-bar **language switcher**; the choice is remembered on the IAM account (`AppUser.language`, `GET`/`PUT /api/iam/me/language`, default English). Backend output, logs and the login screen stay English. **Self-service password change at login**: a "Change password" form on the login screen (English) that also auto-appears when a sign-in reports "account is not fully set up" — it calls the public `POST /api/iam/change-password`, which proves identity by the current password (direct grant, accepting the "not fully set up" state) and sets a permanent password via a least-privilege Keycloak service-account client (`openwcs-iam`), so even temporary/forced-password accounts can recover without an admin; the admin Set-password dialog now defaults Temporary off. The whole SPA is translated: 24 namespaces (~1,700 keys, identical across all five target languages) covering the sidebar nav, every operations / GTP / transport / twin / master-data / settings / slotting / reporting / dashboard / admin / topology screen, and the per-screen help drawer (full body copy, tips and the drawer title translated across every screen, in all five languages). A shared **`useCatalog`** hook resolves entity ids to human-readable codes across operations screens — counting (scope + capture dialog), outbound pick detail, stock-transaction From/To, and transport equipment column all display location codes, SKU codes (with description), and equipment codes rather than raw UUIDs; transport origin/destination/next-hop additionally resolve against GTP station codes (`listWorkplaces`). **Blind count** capture hides the Expected-qty column until reconciled. Inbound/outbound are **read-only** (host owns orders). Shared styled `Select` + searchable/sortable/paginated `DataTable`; warehouse-access user list paginates server-side (Keycloak). **Reporting** sidebar section (5 read-only screens, `/reporting/*`: material flow scan quality + scanner-attention prediction + 3D conveyor traffic heatmap + transit times; ASRS density 90d history + 14d weekday-seasonal forecast + 3D rack-cell movement heatmap + per-device movements; stock per SKU available/allocated/unavailable; inbound + outbound expected/started/active + 90-day dailies + hour-of-day peak maps; recharts charts, lazy 3D scene reusing the editor meshes). **Dashboards** sidebar section (ISA-101 / Few doctrine, design follows `docs/dashboardScope.md`): a **landing situation dashboard** at `/` (the post-login home; the old quick-links dashboard moved to `/home`) with tiles (Stock-blocking top-left, Inbound, Outbound, Dispatch, Automation, Putaway), each a **Hero** (big number + bullet/limit bar vs target + sparkline + ok/warning/critical state colour, with drill-down), plus five menu dashboards (`/dashboards/inbound`, `/outbound`, `/replenishment`, `/stock`, `/abc`: heroes + charts, no tables; ABC has a Pareto chart; Outbound now carries SLA heroes (on-time-to-cutoff %, order-cycle-time median); Inbound carries dock-to-stock + real receive-errors). Two more control-room screens: an **andon board** (`/dashboards/andon`, full-screen alert wall polling `/api/notification/alerts`, critical-first, calm all-clear state) and an **alert-system-health** screen (`/dashboards/alert-health`, ISA-18.2: active-by-severity, opened/cleared per day, chattering + stale alerts from `/api/notification/alerts/health`). Reuses `ui/src/reporting/charts.tsx`; polls 15 s landing / 60 s menu; last-updated stamp + stale grey-out after two failed polls; grey base, `--warning`/`--danger` as the single alert family, no gauges/tables; full i18n. **Settings → Alerts** tab edits the alert thresholds (admin-gated). **AI assistant**: a global floating **chat widget** mounted in the app shell, shown only when `GET /api/assistant/status.enabled`, keeping conversation history + the active warehouse and posting to `/api/assistant/chat`; a **Settings → "AI Assistant"** admin tab to set the Anthropic key (write-only password field), pick the model (Haiku 4.5 / Opus 4.8), and enable the feature; i18n in all languages. Turning **demo mode** on/off also seeds/clears dashboard sample data across services (no standalone button; part of the demo switch). Fixed a React #310 (rendered-more-hooks) crash from a `useMemo` after an early return in the dashboards landing `Overview.tsx` (post-login home, so it crashed broadly) and a second hooks-after-return in the topology `EquipmentMesh`. Per-screen **in-app help** rewritten to a task/worked-example/troubleshooting template for floor operators (`help/content.ts`). Dockerised (nginx) on host **:443 (HTTPS forced)** under compose `--profile apps` (proxies `/api`→gateway, `/realms`+`/admin`→Keycloak); **gateway JWT + warehouse scope enforced**. |
| libs/common | Java | — | ✅ | `EventEnvelope`. |

**Contracts:** OpenAPI ✅ master-data, inventory, txlog, allocation, order-management, iam,
flow-orchestrator, integration-sap, integration-manhattan, host-api, process-engine; ⬜ master-data
shipper/fulfillment-config paths, other services. Avro/Schema-Registry ⬜.

**Platform:** docker-compose ✅ (incl. allocation; Keycloak imports the `openwcs` realm).
**CI ✅** (GitHub Actions: Java build+test with Testcontainers, Go adapters, UI build, OpenAPI
validation). **Gradle wrapper committed.** **k8s starter manifests ✅** (`deploy/k8s/`; horizontal
scaling — see [`SCALING.md`](./SCALING.md)); Helm ⬜.

**Observability:** decision-point logging convention (INFO with codes next to ids and the
"because ..." trigger, WARN for skipped/degraded paths with the consequence, DEBUG in loops)
applied ✅ all services (Java + Go).

**Public sandboxed live demo ✅** (fully client-side, no backend): a `VITE_DEMO=true` build of the
existing `/ui` SPA (`npm run build:demo`, asset base `/demo-app/`) embedded on the marketing site at
**`/live-demo`**. Behind the flag: a synthetic read-only session (no Keycloak, all menus visible via
`can()`, all writes off via `canWrite()`/`writeAllowed()`); a mock service layer
(`ui/src/demo/mockApi.ts`) consulted by the single `authFetch` chokepoint so **no `/api`, `/admin` or
`/realms` request ever leaves the browser** (verified); GET responses from committed fixtures snapshotted
from `app.openwcs.ai` (`ui/src/demo/fixtures/*.json`, captured by `tools/demo-snapshot.mjs`), unknown
GETs return benign empty states, mutations are no-ops except the GTP pick endpoints; an in-memory pick
engine (`ui/src/demo/pickEngine.ts`) arms **5 totes** at the goods-to-person station (induction queue
REQUESTED → IN_TRANSIT → QUEUED, present, confirm full/short, close cycle, next tote, dirty-tote /
broken-units exceptions, `reset()`); a `DemoBanner` ("Live demo, sample data, nothing is saved" +
Restart). Nothing persists (a reload resets). The **Reporting screens** (Stock, ASRS, Material flow,
Inbound/Outbound) are now mocked too (inventory stock-by-sku + storage-density, flow scan-quality /
traffic / transit-times / storage-movements / device-movements, orders flow by direction) so they
render with data instead of a blank screen; `emptyFor` was hardened so any unmapped report path
returns `[]` (the blank Stock screen was a crash on spreading an object as an array). The public
Express server serves the bundle at **`/demo-app/`** from `public/static/demo` (committed generated
output, rebuilt for this change); from `/public`, `npm run build:demo-app` builds and copies it. **CI runs `npm run build:demo`** so the demo build cannot silently break. Spec
`docs/public-live-demo-spec.md`; AS-BUILT §7j.

---

## 2. Roadmap progress (build.md §15)

| Phase | Status | Detail |
|---|---|---|
| **0 — Foundations** | ✅ | Repo + compose + shared schemas + txlog/outbox/relay + Kafka ✅; IAM model + gateway JWT + per-endpoint RBAC (all services) + inter-service identity propagation ✅ (toggleable); **CI ✅ (green), Keycloak `openwcs` realm ✅, gradle wrapper ✅**; **JWT edge-auth path exercised end-to-end against a live Keycloak realm (Testcontainers) ✅**. Remaining hardening: mTLS between services. |
| **1 — Master data + inventory MVP** | ✅ | Master Data ✅, Inventory projection ✅, log→projection loop proven ✅. |
| **2 — Process engine + one equipment family** | ✅ | flow-orchestrator device-task lifecycle + uniform device contract ✅, conveyor adapter ✅, DEVICE RBAC ✅, **process-engine (Flowable BPMN) ✅ with a sample goods-in process that originates a device task** ✅. Gap: a process designer UI + richer processes. |
| **3 — Outbound + more equipment** | 🟡 | **order-management ✅, allocation + cubing + batch picking + release management ✅, inventory reservation/ATP ✅, dispatch labels/services/routes ✅ (incl. integration-sap label-barcode + route feed).** Gaps: host-integration gateways translate into the canonical Host API but the real SAP/Manhattan wire protocols (OData/BAPI/IDoc, Manhattan REST) are still skeletal ⬜; more adapters ⬜. The **BPMN outbound process** is now ✅ (process-engine: release → allocate → gateway on fulfillability → pick/dispatch → route). |
| **3b — Inbound slotting & replenishment** | 🟡 | **Put-away engine ✅ (velocity-to-exit, soft single-SKU-per-lane, aisle redundancy + balance), HU type capabilities + per-area allowed-HU-types ✅, empty-HU far placement + LOW transport priority ✅, multi-compartment HUs (dominant velocity + SKU-set affinity) ✅, cell-as-location coords aisle/side/x/y/z ✅, manual pick-face min/max + opportunistic replenishment ✅, off-peak re-slotting ✅, self-taught recency-weighted ABC ✅, slotting UI ✅, goods-in put-away delegate ✅, live-inventory occupancy filter on put-away candidates (drops physically-occupied slots before scoring; best-effort if inventory is down) ✅** (ADR 0003). **Relocation plan endpoint ✅** (ADR-0009 Phase 1: `POST /api/slotting/relocation-plan`, dig-out sequencing for multi-deep shuttle channels). **Profile-less put-away fallback ✅: a SKU without a storage profile resolves to the warehouse's only automated block** (`SHUTTLE_ASRS`/`CRANE_ASRS`/`AUTOSTORE`/`AMR_GTP`; several candidates still 400 — a profile must disambiguate). **Physical move dispatch ✅** (execution layer): slotting plans now dispatch as real flow moves — flow `POST /api/flow/moves` dispatches a RELOCATE / AutoStore BIN_RELOCATE device task, books the HU's new inventory location on completion, and the `HandlingUnitMoved` audit fires (stock follows the HU, so no `StockMoved` for HU-bound moves = no double-apply); slotting's `FlowClient` + dispatch endpoints (`reslot/{id}/dispatch`, `replenishment/{taskId}/dispatch`, `decant/putaway`) flip plans to DISPATCHED (`V7` `device_task_id`); the **decant→putaway seam is done**; the dormant **`StockMoved`/`PutawayCompleted` projection is proven for loose stock**. Gaps: the multi-leg RETRIEVE→CONVEY→STORE move ⬜ (RELOCATE single-leg done), lane-depth ledger↔inventory reconciliation ⬜, deep-lane cell grouping ⬜. |
| **3c — Goods-to-person station execution** | 🟡 | **GTP station + node config, order-destination demand, present-stock → put-to-light put-list (batch: one stock HU → many orders), confirm/short puts, destination completion; ORDER_LOCATION + PUT_WALL topology ✅; orthogonal operating modes (PICKING / DECANTING / STOCK_COUNT / QC / MAINTENANCE) with a generalised task-line work-cycle + outcomes ✅; deactivate/drain control + re-activate ✅; in-transit capacity caps per mode class ✅; topology-projected station nodes (`POST /stations/{id}/nodes/sync`, `inboundDistanceM`) ✅** (ADR 0006). **Phase 3c-1 ✅: flow-owned induction queue** (`REQUESTED → IN_TRANSIT → QUEUED → DONE`, `arrival_seq`, cap metered at RETRIEVE dispatch; `induction_queue_entry` + `hu_transport_trace` in flow schema; `POST /api/flow/induction/requests`, `GET /api/flow/induction/queue`, `POST /api/flow/induction/entries/{id}/done`, `GET /api/flow/hu-trace`; GTP operator console shows `REQUESTED`/`IN_TRANSIT`/`QUEUED` pipeline; transport screen shows per-HU trace timeline; counting uses single `requestPresentation` call). **Phase 3c-2 ✅: emulator loop recirculation** (`OPENWCS_EMULATOR_RECIRC_EVERY=N`; `recirc.go`): every Nth CONVEY recirculates once, diverging arrival order from dispatch order (R2); CONVEY result carries `recirculations` + `decisions` (sorter `RECIRCULATED`/`DIVERTED`); flow's `onConveyCompleted` writes decision points to HU trace before ARRIVED (R4); `/config` gains `recircEvery` (live-tunable). **Phase 3d ✅ (ADR-0008): live scan-driven conveyance** — flow assigns route plans on each CONVEY dispatch + records `SCANNED` trace rows; emulator walks graph at 0.5 m/s obeying flow routing decisions; twin replays the real scan trail; return-to-storage CONVEY resolved via the routing graph (`LiveScanConveyanceTest`). **Twin AMR/AutoStore live views ✅** (execution layer): equipment-emulator exposes `GET /amr/fleet` + `GET /autostore/grid` telemetry, flow maps it to world coords (`GET /api/flow/twin/amr-fleet`, `GET /api/flow/twin/autostore`, `TwinTelemetryServiceTest`), and the twin scene renders the AMR fleet (status-coloured robots, carried HU on hover) + AutoStore ports + a fill % stat (`twin_test.go`); metre-exact continuous crane motion still pending. **ADR-0009 ✅ (Phases 1 + 2): multi-deep channel relocation** — slotting plans the dig-out order (`relocation-plan`); flow dispatches RELOCATE chain, books + traces each blocker move, then issues RETRIEVE when the channel is clear (`RelocationPlanApiTest`). **Picking execution UI ✅** (execution layer): a guided RF-style **Picking** operator screen (`/picking`) over order-management's pick queue (`GET /api/orders/pick-tasks`) + per-line confirm/short (`POST /api/orders/pick-tasks/{lineId}/confirm` → a `Picked` line transaction via outbox→txlog, decrements stock, marks SHORT); pick-by-light / voice / RF are now hardware seams layered on this screen. Gaps: physical put-lights + picking-HU retrieval (device adapters/flow-orchestrator) ⬜, allocated pick-location threaded onto the line (so `pick-tasks` `locationId` is never null) ⬜, demand auto-wire from allocation batches ⬜, count→inventory-StockAdjusted wiring ⬜, gtp `station_queue_entry` table removal ⬜. |
| **4 — Counting & operations** | 🟡 | `StockAdjusted` projection ✅; **cycle/stock counting service ✅** (`counting`: count tasks, ABC-cadence schedule, blind/variance, recount + reconciliation → `StockAdjusted`); **Count-tote routing ✅** (emulator mode: counting calls `flow.requestPresentation`; flow orchestrates RETRIEVE + CONVEY to an active STOCK_COUNT station, dispatching to the correct adapter family — ASRS/AUTOSTORE/AMR; cap metered at RETRIEVE dispatch; best-effort); **GTP store-back family resolution ✅** (store-back STORE transport dispatched to the adapter family of the destination's storage type); **Dashboards & alerting ✅** (landing situation dashboard + five menu dashboards — inbound/outbound/replenishment/stock/ABC — over new read-only warehouse-scoped aggregation endpoints in order-management/inventory/allocation/slotting/flow; the **notification** service is now functional with a ShedLock-guarded ~60 s threshold evaluator that opens/dedupes/clears WARNING/CRITICAL alerts and delivers them by email + webhook; admin alert-threshold settings in master-data + Settings → Alerts); **dashboards tail ✅** (the Phase-4 tail in `dashboardScope.md`: real **SLA metrics** `GET /api/orders/reports/sla` surfaced as Outbound heroes; a full-screen **andon board** `/dashboards/andon`; the ISA-18.2 **alert-system-health** view `/dashboards/alert-health` over `GET /api/notification/alerts/health`; plus the data-gap fixes — real inbound receive-errors, **true windowed ABC** risers/fallers from `sku_pick_daily` (no more EWMA proxy), and **dock-to-stock** timing from `handling_unit.stored_at`; demo mode now seeds/clears all dashboard sample data); **at-station blind count UI ✅** (GTP ops `CountPanel` — GTP-routed totes only); **general operator count capture UI ✅** (`ui/src/counting/CountingScreen.tsx`: ad-hoc + scheduled count tasks, per-line count capture, variance review, recount, reconcile; count-tote routing status persisted with failed ASRS routing retried every minute). |
| **5 — Hardening & scale** | ⬜ | DLQs, circuit breakers, replay tooling, perf, security review. |

---

## 3. Test coverage (implemented; not yet run here)

| Service | Tests | Kind |
|---|---|---|
| master-data | `MasterDataPersistenceTest`, `MasterDataApiTest`, `MasterDataRbacTest`, `DispatchCatalogApiTest`, `LabelTemplateApiTest`, `HostManagedMasterDataTest`, `SkuSyncApiTest`, `EmulatorModeTest`, `AdminDbConsoleTest`, `DemoModeResetTest`, `OperationalLocationTest`, `StockRulesTest`, `AlertThresholdsTest`, `AiAssistantConfigTest` | Testcontainers + MockMvc (incl. RBAC: read=VIEW, write=EDIT; shipping-service + route catalogs; label-template CRUD + ZPL/PDF render; host SKU sync upsert semantics (absent SKUs untouched; within a synced SKU nested UoMs/barcodes fully replaced); **hardware-emulator flag**: defaults OFF, enable/disable ADMIN-gated, `GET /emulator` reflects state; **admin DB console** (access governed by the gateway via `admin-database` screen access, not the controller): schema listing, SELECT happy path, UPDATE/INSERT/multi-statement/CTE-write rejection, row cap + truncated flag, smuggled write blocked by the READ ONLY transaction; **demo-mode full reset** (`DemoModeResetTest`): disable clears demo catalog + shippers/HU type via bulk DELETEs, unsets cubing default-shipper FK first, warehouses/locations/topology kept; **operational locations** (`OperationalLocationTest`): lazily created on first use, idempotent under concurrency via `(warehouse_id, code)` unique constraint + retry-fetch; **stock rules** (`StockRulesTest`): `single-sku-per-compartment` defaults ON, ADMIN-only enable/disable toggle round-trip; **alert thresholds** (`AlertThresholdsTest`): `GET /alert-thresholds` returns the documented defaults, admin `PUT` round-trips into `system_configuration` key `DASHBOARD_ALERT_THRESHOLDS`, non-admin `PUT` rejected; **AI assistant config** (`AiAssistantConfigTest`): public `GET /api/master-data/ai-assistant` returns `{enabled, configured, model}` and never the key, admin `PUT` sets the key + model + enabled (omitting `apiKey` keeps the existing key), the network-only `GET /internal/ai-assistant` returns the actual key, non-admin `PUT` rejected) |
| assistant | `AnthropicChatModelToolLoopTest`, `AssistantControllerTest` | Mockito + MockMvc (`AnthropicChatModelToolLoopTest`: the Messages-API tool-use loop dispatches read-only tool calls, forwards the caller identity + warehouse scope on each, feeds tool results back, and answers from tool results only; `AssistantControllerTest`: `/api/assistant/chat` round-trip + `/api/assistant/status` reports enabled/configured/model, disabled when no key is set) |
| txlog | `TransactionLogServiceTest`, `OutboxRelayTest` | Testcontainers + Mockito |
| inventory | `InventoryPersistenceTest`, `StockProjectionServiceTest`, `InventoryServiceTest`, `OccupiedLocationsTest`, `HandlingUnitLocationTest`, `UnknownLocationAllocationTest`, `DemoSeedServiceTest`, `DemoClearRaceTest`, `StockBySkuReportTest`, `StorageDensityReportTest`, `InventoryDashboardReportTest`, `DemoDashboardSeedTest` | Testcontainers (incl. per-location occupancy: a stock-row location and a handling-unit location both report occupied, an empty one does not; **HU location registry**: create, update, clear via `PUT /api/inventory/handling-units/{id}/location`; **HU contents** (`HandlingUnitLocationTest`): `GET /api/inventory/handling-units/{id}/contents` returns the stock rows riding the HU; **loose-stock move projection** (`StockProjectionServiceTest`): a `StockMoved`/`PutawayCompleted` event for loose (non-HU) stock moves the qty from→to via the previously-dormant projection branch (HU-bound moves stay carried by the registry booking — no double-apply); **unknown-location allocation** (`UnknownLocationAllocationTest`): null-booking redirects HU + stock to the UNKNOWN operational location, UNKNOWN stock excluded from ATP and can never be reserved; **demo seed** (`DemoSeedServiceTest`): sample HUs seeded with one SKU per compartment, 50 empty HUs included, seed rejected when demo mode off; **demo-off clear is location-blind + race-safe** (`DemoClearRaceTest`): the warehouse-scoped clear removes HUs on operational locations (conveyor segment, station) just like ASRS-slot HUs, and a transport-lifecycle location booking that races the clear throws on the deleted, `@Version`-ed row and never re-inserts it; **stock-by-SKU report**: available/allocated/unavailable split incl. UNKNOWN-location stock, over-allocation floors at 0, answers without the split when master-data is down; **storage density**: idempotent per-block-per-day snapshot, on-demand today, window cut-off, zero-cell block; **dashboard aggregate** `InventoryDashboardReportTest`: `/reports/dashboard` {husReceivedToday,huCount,skuCountWithStock,utilisationPct,asrsUtilisationPct,putawayBacklog (oldest age),dockToStock} — dock-to-stock median from `handling_unit.stored_at` (received→stored today), backlog age from HUs received but not yet stored; **demo dashboard seed** `DemoDashboardSeedTest`: `/demo/seed-dashboard` adds HUs with backdated received/stored timings (dock-to-stock) plus a couple parked at receiving (null `stored_at` = backlog)) |
| allocation | `AllocationEngineTest`, `AllocationServiceTest`, `StockBlockingReportTest` | Pure logic (incl. multi-size cubing: largest-first + line split across cartons with `lineNo`/`shipperUnitId` links) + Testcontainers (allocate → cancel releases reservations; oversized SKU → `CUBING_FAILED` + reservation released; per-carton dispatch labels with a host barcode per shipper; **allow-short**: available qty reserved + held, zero-stock line fully short, only allocated quantities cubed, strict mode still releases everything; **pick location on the line** (`AllocationServiceTest`): a FULFILLABLE allocation returns the primary allocated pick `pickLocationId` per line; **stock-blocking dashboard** `StockBlockingReportTest`: `/reports/stock-blocking` {blockedLines,distinctSkusShort,recoverableLines,zeroStockLines,openOutboundLines}) |
| order-management | `OrderTransactionTest`, `OrderTransactionRelayTest`, `OrderAuthorizationTest`, `OrderReleaseShortTest`, `PickTaskTest`, `PickTaskAuthorizationTest`, `DemoSeedTest`, `OrderFlowReportTest`, `OrderDashboardReportTest`, `OrderSlaReportTest`, `DemoDashboardSeedTest` | Testcontainers + Mockito (**pick tasks** `PickTaskTest`: `GET /api/orders/pick-tasks` returns released/allocated outbound lines needing picking ordered for a pick walk, and `POST /api/orders/pick-tasks/{lineId}/confirm {pickedQty, short?}` posts a `Picked` line transaction through the outbox→txlog, advances `pickedQty` and marks SHORT; `pickQueueShowsAllocatedPickLocation` — a line carrying a persisted `pick_location_id` surfaces it as the queue entry's `locationId` (real location, not null); `PickTaskAuthorizationTest`: read gated on `ORDER_VIEW`, confirm gated on `ORDER_POST_TRANSACTION`; outbox, relay, and per-endpoint RBAC: VIEWER 403 / SUPERVISOR 201; **release-short**: OPERATOR 403, transition to `PARTIALLY_ALLOCATED` with per-line short quantities, 409 when not short / nothing available, ship of a short order stages `OrderShipped` with shipped < ordered; **demo seed** creates 10 sample INBOUND/OUTBOUND orders when demo mode on, rejected when off; **order-flow report**: seeded INBOUND/OUTBOUND orders across statuses + backdated timestamps produce the documented expected/active/started figures, per-day buckets and hour-of-day intake; **dashboard aggregates** `OrderDashboardReportTest`: `/reports/dashboard` {inbound,outbound,perHour} (incl. real `receiveErrorsToday` from over/under receipts) and `/reports/dispatch` {nextDeparture,routes[]} from seeded orders + routes; **SLA report** `OrderSlaReportTest`: `/reports/sla` on-time-to-cutoff % (shipped at/before `dispatchBy`) + order-cycle-time median (created→shipped) + per-day series; **demo dashboard seed** `DemoDashboardSeedTest`: `/demo/seed-dashboard` backfills backdated orders across statuses + routes + on-time/late + receive-errors, gated on demo mode) |
| iam | `IamServiceTest` | Testcontainers (seeded roles, effective permissions, catalog validation) |
| notification | `AlertEvaluatorTest`, `AlertApiTest`, `AlertHealthApiTest`, `DemoDashboardSeedTest` | Testcontainers + mocked metric clients (`AlertEvaluatorTest`: a metric breaching its threshold opens a WARNING/CRITICAL alert, a repeat evaluation dedupes by (warehouse, area, metric) rather than re-opening, the alert clears when the metric falls back under, and open/clear trigger email + webhook delivery (degraded-to-log when SMTP unset); `AlertApiTest`: `GET /api/notification/alerts?warehouseId=` lists open alerts, `POST /api/notification/alerts/{id}/ack` is SUPERVISOR/ADMIN-gated; **`AlertHealthApiTest`**: `/alerts/health` returns active-by-severity, per-day opened/cleared, chattering (many flaps on one key) and stale (long-open) alerts over the window; **`DemoDashboardSeedTest`**: `/demo/seed-dashboard` writes active + 14d historical + chattering + stale `DEMO-`-marked `alert_event` rows and `/demo/clear` removes only those) |
| slotting | `RelocationPlanApiTest`, `PutawayServiceTest`, `BlockOccupancyDistanceTest`, `DashboardApiTest`, `DispatchServiceTest`, `DemoDashboardSeedTest` | MockMvc + mocked clients + Testcontainers (**dispatch** `DispatchServiceTest`: `reslot/{id}/dispatch` + `replenishment/{taskId}/dispatch` + `decant/putaway` call flow `POST /api/flow/moves` and flip the plan to DISPATCHED with the returned `device_task_id`, decant runs the put-away scorer first, and a flow failure leaves the plan un-dispatched (best-effort); **relocation plan** `RelocationPlanApiTest`: clear channel → empty steps; single blocker → one step with correct from/to; chained blockers → ordered steps all sharing the same `cellY`; **put-away service** `PutawayServiceTest`: velocity scoring, lane consolidation, aisle-redundancy cap, assignment lifecycle — supersede on reassign + STORED confirmation against real Postgres; **block occupancy distance** `BlockOccupancyDistanceTest`: single-aisle block second putaway succeeds after the aisle-share cap is lifted for single-aisle blocks; **dashboard aggregates** `DashboardApiTest`: `/replenishment/dashboard` {urgent,openTasks,oldestAgeMin,projectedStockouts} and `/velocity/abc` {a,b,c,pareto,top,bottom,risers,fallers} — **windowed cases**: `sku_pick_daily` concentrated in the last 14 days surfaces a SKU as a riser, older-only tallies as a faller, exact trailing-14d-vs-90d (the EWMA proxy is gone); **demo dashboard seed** `DemoDashboardSeedTest`: `/demo/seed-dashboard` writes `sku_velocity` + 90d `sku_pick_daily` (riser/faller skew) + `replenishment_task` rows, gated on demo mode) |
| flow-orchestrator | `DeviceTaskServiceTest`, `RoutingEngineTest`, `RoutingServiceTest`, `DiscoveryServiceTest`, `InductionQueueServiceTest`, `LiveScanConveyanceTest`, `ReportingTest`, `AutomationSummaryTest`, `TwinTelemetryServiceTest`, `FlowMoveChainServiceTest`, `DemoDashboardSeedTest` | Testcontainers + Mockito (device tasks; pure next-hop; routing through targets; loop HOLD/OVERFLOW; topology learning; **`InductionQueueServiceTest`**: RETRIEVE callback → `IN_TRANSIT` + dispatches CONVEY; CONVEY callback → `QUEUED` with `arrival_seq` in arrival order (incl. out-of-request-order case proving R2); queue read includes `REQUESTED`; cap counts only `{IN_TRANSIT,QUEUED}`; one trace row per write point; DONE endpoint idempotent; **Phase 3c-2**: `conveyDecisionPointsAreWrittenToTheTrace` — emulator recirculation payload (`RECIRCULATED`+`DIVERTED` decisions) is written to the HU trace before ARRIVED; **Phase 3d**: `LiveScanConveyanceTest` — CONVEY with `entryNode` triggers route-plan assignment + `SCANNED` trace rows; scan-loop HOLD; return-to-storage CONVEY entry/destination resolved from projected graph; **`ReportingTest`**: scan/no-read/unknown counters accumulate into one row per node+day; ROUTE answers (incl. divert defaults) count edge traffic; day-window filtering; storage/device movements aggregated from seeded device tasks; transit-time p50/p95 from trace pairs; decision-latency ring-buffer percentiles after 200 decides; **`RoutingFastPathTest`**: graph-snapshot eviction on editor replace AND on topology projection (next decide routes the new graph); precomputed next-hop tables equal the old per-scan Dijkstra on a non-trivial graph (unique-path costs, all node pairs); async side effects eventually persist (polling assert); warm decide() p50 under 10 ms over 200 planned scans with measured numbers printed; **physical-move audit** (extended `DeviceTaskServiceTest`, `TxLogClient` mocked): a completed STORE emits `HandlingUnitMoved` with the destination location, RETRIEVE records the source, RELOCATE records both ends; the async path audits exactly once on callback (not on dispatch, not on a duplicate callback); a FAILED move is audited with status FAILED; CONVEY is not audited; a throwing txlog never fails the move; **physical move dispatch** (`DeviceTaskServiceTest` move cases): a same-system `POST /api/flow/moves` dispatches a RELOCATE (AutoStore → BIN_RELOCATE) device task with the from/to locations, the completion callback books the HU's new inventory location, and no `StockMoved` is emitted for the HU-bound move; **multi-leg moves** `FlowMoveChainServiceTest`: a cross-system `POST /api/flow/moves` (locations in different storage blocks) starts a RETRIEVE→CONVEY→STORE chain tracked in `flow_move_chain` (`V19`), each leg's callback advances the chain idempotently (a re-fired callback never double-advances), and the final STORE books the destination + fires `HandlingUnitMoved`; **twin telemetry** `TwinTelemetryServiceTest`: emulator AMR-fleet + AutoStore-grid telemetry maps to world coords for `/twin/amr-fleet` + `/twin/autostore`, degrading to empty when the emulator is off; **automation-summary dashboard** `AutomationSummaryTest`: `/reports/automation-summary` {scanNoReadPctToday,equipmentAvailabilityPct,equipmentTotal,equipmentInFault} from seeded scan stats + device tasks; **demo dashboard seed** `DemoDashboardSeedTest`: `/demo/seed-dashboard` writes today's `scan_stat` (one elevated no-read node) + a faulted device, and demo-clear purges them) |
| equipment-emulator | `tasks_test.go`, `walk_test.go`, `twin_test.go` | Go httptest (**twin telemetry** `twin_test.go`: `GET /amr/fleet` reports simulated robot positions/status/carried-HU and `GET /autostore/grid` reports grid fill + port activity, both updated by AMR/AutoStore task processing; `POST /tasks`: COMPLETED with realistic latency, simulated duration reported; **fault injection** — `faultEvery=2` fails every 2nd task with FAILED + `fault: true`, alternates deterministically; **loop recirculation** — `recircEvery=2` recirculates every 2nd CONVEY deterministically (`TestConveyJourneyRecirculatesDeterministically`); a recirculating CONVEY task reports `recirculations=1` + two-entry `decisions` ([RECIRCULATED, DIVERTED]) in the result payload (`TestConveyTaskReportsDecisions`); **`/config` endpoint** — POST updates `latencyOverrideMs`/`faultEvery`/`recircEvery`/`speedMps` atomics, GET reflects them; 405 on GET `/tasks`; **`walk_test.go`**: live-walk CONVEY scans every graph node and obeys flow's routing decisions; HOLD causes rescan of the same node; tasks without `entryNode` fall back to atomic sleep) |
| adapters/conveyor | `main_test.go` | Go httptest (`POST /tasks`: FAILED "hardware not connected" on all commands, 405 on GET) |
| adapters/{asrs,amr-geekplus,autostore} | `tasks_test.go` | Go httptest (`POST /tasks`: FAILED "hardware not connected" for all families) |
| adapters/ptl | `main_test.go` | Go httptest (`POST /tasks`: ILLUMINATE lights a light + reflected in `GET /state`, CLEAR turns it off, unsupported command + missing `lightId` → FAILED) |
| adapters/conveyor-sniffer | `sniffer_test.go` | Go (decoder; IP allowlist; ingest→decode→forward end-to-end; HTTP observation post) |
| gateway | `GatewayAuthEndToEndTest` | Testcontainers (live Keycloak + imported `openwcs` realm): no token → 401, realm JWT → 200 + identity propagated, client-supplied `X-Auth-*` stripped (anti-spoof) |
| integration-sap | `LabelControllerTest`, `RouteFeedControllerTest`, `SapOrderControllerTest` | MockMvc (label-barcode; route-feed upsert; SAP order → Host API translation with material→SKU + unknown-material 422) |
| integration-manhattan | `ManhattanOrderControllerTest` | MockMvc (Manhattan order → Host API translation with item→SKU + unknown-item 422) |
| integration-host | `HostControllerTest`, `ConfirmationControllerTest`, `HostReferenceControllerTest`, `HostInventoryControllerTest`, `IdempotencyFilterTest`, `WebhookDispatcherTest` | Testcontainers + MockMvc + mocked clients (order/ASN mapping; confirmations cursor feed; SKU sync with UoMs + barcodes; adjustment → StockAdjusted append; `Idempotency-Key` replay; webhook push advances cursor) |
| process-engine | `ProcessEngineTest`, `OutboundProcessTest` | Testcontainers + Flowable (goods-in dispatches a device task; outbound releases → user task → dispatch, exercising delegates + a wait task; an `allowShort=true` instance short-releases and `FULFILLABLE_SHORT` flows the happy path) |
| gtp | `GtpContextTest`, `WorkCycleExecutionTest`, `OperatingModesTest`, `StationQueueTest`, `StationCapacityTest`, `StationExceptionTest`, `DecantCompartmentRuleTest`, `PickLayoutTest` | Testcontainers (entity↔schema round-trip; one stock HU serves many destinations = batch; confirmations decrement + complete; short HU leaves surplus demand OPEN; short confirm → SHORT; BOTH topologies — ORDER_LOCATION + PUT_WALL — produce correct lit put instructions; **each operating mode** — DECANTING moves source→target + exposes put-away seam, STOCK_COUNT computes variance + exposes StockAdjusted seam, QC records PASS/FAIL/HOLD, MAINTENANCE records OK/DEFECTIVE/REPAIR; unsupported-mode rejected; setSupportedModes retains PICKING; **`StationQueueTest`** (ADR-0007 Phase 3c-1): completion fans out to flow `done` + store-back; drain rejection; **`setCapacity`**: caps reflected in station view; **`StationExceptionTest`**: store-back dispatches to the correct adapter family (`storeBackDispatchesToTheDestinationStorageFamily` — AUTOSTORE destination → `AUTOSTORE` asserted on `createTransport`; existing store-back test updated to stub `SHUTTLE_ASRS` → `ASRS`); **`DecantCompartmentRuleTest`** (stock rules): same-compartment mix rejected, capacity overflow (more distinct SKUs than compartments) rejected, multi-compartment decant allowed, rule OFF allows mixing, unresolvable HU type still enforces the per-compartment check) |
| counting | `CountingServiceTest`, `DemoSeedTest`, `CountRoutingTest` | Mockito + Testcontainers + mocked clients (`CountingServiceTest`: delete accepted for OPEN task, rejected with `IllegalStateException` once counted; **demo seed** creates sample count tasks over demo stock when demo mode on, rejected when no stock; **count-tote routing** (ADR-0007 Phase 3c-1): emulator ON + ASRS cell + active station → `flow.requestPresentation` called (one call; no separate gtp.enqueue or flow.createTransport); **multi-family routing**: AutoStore cell → `AUTOSTORE` family asserted on `requestPresentation`; emulator OFF → no routing; routing failure → count task still created; **at-station blind count** (4 tests): first count == expected → ACCEPTED no adjustment; two agreeing counts ≠ expected → ADJUSTED (delta posted, reason COUNTING, task RECONCILED, **`huId` asserted on adjustment — targets tote bucket not phantom HU-less row**); third count after two mismatches → ADJUSTED; recount == expected → ACCEPTED without adjustment) |

---

## 4. Known gaps, caveats & tech debt

- **Uncompiled** — biggest caveat; run the test suite before trusting any of it.
- **`ddl-auto: validate` + JSONB** — usually fine on Hibernate 6; fallback is
  `ddl-auto: none` (tests still cover mappings).
- **No auth** anywhere.
- **Cubing** is volume+weight greedy (not 3D); it packs across multiple shipper sizes
  (largest-first, downsizing the last carton) but doesn't optimise for fewest cartons.
- **Pick-type breakdown** assumes base-UoM stock and reads case size from the "CASE" UoM;
  SPLIT_CASE is treated as eaches for quantity.
- **Allocation↔services** calls are synchronous REST; partial-failure compensation exists
  in the allocator but cross-service consistency is best-effort (no saga/outbox there yet).
- **Audit `actor`** is required on stock transactions + every logged event, but is
  caller-asserted until IAM/JWT supplies an authenticated principal.
- **Dirty-tote vs flow's return leg**: marking a tote dirty (maintenance) still triggers flow's
  return-to-storage leg — gtp can no longer (and never effectively could) suppress it; a
  "complete without return" flag on flow's `done` endpoint is a follow-up.
- Order status is not auto-advanced by postings (no auto-complete on `postedQty` ≥ `qty`).
- Events only on `txlog.stream`; no consumer-driven contract tests (CI does validate the
  OpenAPI specs structurally). First CI run may surface compile errors not catchable locally.

---

## 5. Suggested next steps

1. **Stand up a Keycloak `openwcs` realm** + `gradle wrapper` so the JWT + RBAC path is
   exercisable end-to-end (enforcement is wired across all services but only verifiable with a
   realm). Consider resolving custom IAM roles at runtime and mTLS between services.
2. **End-to-end MockMvc tests** across the outbound slice (release → allocate → ship → cancel)
   and the inbound/count/adjust posting + relay flow.
3. **master-data catalog events** + shipper/fulfillment-config paths in `master-data.yaml`.
4. **Order auto-complete** when a line is fully posted (`postedQty` ≥ `qty`).
5. **process-engine (Flowable BPMN)** + goods-in/outbound processes that drive the
   flow-orchestrator device tasks (Phase 2 increment 2).

> Done since last revision: **Phase 2 increment 1** — flow-orchestrator now owns the device-task
> lifecycle over the uniform device contract (build.md §8): `POST/GET /api/flow/device-tasks`,
> the `flow.device_task` store, family→adapter routing via `HttpDeviceClient`, and DEVICE_VIEW /
> DEVICE_OPERATE RBAC. The **conveyor adapter** gained a `POST /tasks` simulator. `DeviceTaskServiceTest`
> (Testcontainers) and `main_test.go` (Go) added; `flow-orchestrator.yaml` OpenAPI spec added.
> The device contract is synchronous HTTP for now; async Kafka (`device.tasks`/`device.results`)
> is the production target.
