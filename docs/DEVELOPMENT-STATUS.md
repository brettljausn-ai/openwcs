# openWCS — Development Status

_Last updated: 2026-06-12 (flow routing fast path: in-memory graph snapshot + precomputed next-hops + async scan side effects + decision-latency report endpoint)_

Live status of the build against the roadmap in [`build.md` §15](../build.md). For what
the implemented parts actually do, see [`AS-BUILT.md`](./AS-BUILT.md).

**Legend:** ✅ functional · 🟡 partial · 🟦 scaffold (health/info only) · ⬜ not started

> ⚠️ Code is authored without a local JVM/Gradle, so it is not compiled here. **GitHub
> Actions CI** (`.github/workflows/ci.yml`) is now the verification gate: it runs
> `./gradlew build` (Testcontainers tests on Docker), builds the Go adapters + UI, and
> validates the OpenAPI specs. Watch the first run for compile errors that couldn't be
> caught locally.

---

## 1. Component status

| Component | Lang | Port | Status | Notes |
|---|---|---|---|---|
| gateway | Java | 8080 | ✅ | Routes `/api/<service>/**`; JWT validation (toggleable) + forwards/strips X-Auth-* identity. |
| master-data | Java | 8081 | ✅ | Catalog CRUD + outbound config: shippers, fulfillment config (pick types, cubing mode, batch config); dispatch reference data: shipping-service + route catalogs, label templates (+ ZPL/PDF render). **Host SKU sync** (`POST /skus/sync`): upsert (not full-catalog replace) — SKUs absent from the batch are left untouched; within each synced SKU the host is authoritative and its nested UoMs/barcodes fully replace what is stored. Whole batch in one transaction. **System config** flags in `system_configuration`: demo mode (`/api/master-data/demo`; disable = full reset via bulk DELETEs — whole SKU catalog + demo shippers/HU type, cubing default-shipper FK unset first; UI also clears inventory/orders/counting/flow/gtp per warehouse and wipes the txlog journal, surfacing failed clears) and **hardware emulator** (`HARDWARE_EMULATOR_ENABLED`, `/api/master-data/emulator` get/enable/disable, ADMIN-gated, **OFF by default**); **stock rules** (`/api/master-data/stock-rules`, ADMIN-gated writes, **single-SKU-per-compartment ON by default** — enforced by GTP decanting); polled by the `equipment-emulator` service — when ON, flow-orchestrator routes device tasks there instead of the real adapters. **Admin database console** (`/api/master-data/admin/db`, ADMIN-gated): `GET /schemas` (non-system schemas + tables + columns) and `POST /query` running exactly one SELECT — validator (masked literals/comments, multi-statement + write/DDL keyword rejection incl. CTE-smuggled writes) plus a READ ONLY rolled-back transaction, 10 s statement timeout, row cap 200/1000 with `truncated` flag; audit at INFO without SQL text. **Operational locations** (`GET /locations/operational?warehouseId=&kind=EQUIPMENT|WORKPLACE|UNKNOWN&name=`): lazily-created per-warehouse locations so HUs always book to a real place (EQUIPMENT → `CONVEYOR_SEGMENT`/`TRANSPORT`, WORKPLACE → `STATION`/`STAGING`, UNKNOWN → `FREE_SPACE`/`QUARANTINE`); idempotent under concurrency via the `(warehouse_id, code)` unique constraint + retry-fetch (`OperationalLocationTest`). **SKU card** (`GET /skus/{id}/card?warehouseId=`): one-call read of SKU identity + base-UoM item dimensions/weight + the warehouse profile's metadata, for operator screens (GTP tote panel). |
| inventory | Java | 8082 | ✅ | Stock projection + SKU- and location-scoped availability/reservations. Per-location occupancy (`POST /api/inventory/locations/occupied`): which of a set of locations physically hold any stock row or handling unit (used by slotting put-away). **HU location registry** (`PUT /api/inventory/handling-units/{id}/location`): flow books (or clears) a handling unit's current storage location through the transport lifecycle so the registry stays live; consumed by ADR-0009 dig-out occupancy checks (`HandlingUnitLocationTest`). **Null bookings now go to the warehouse's UNKNOWN operational location** (HU + riding stock rows; resolved via a per-warehouse-cached `MasterDataClient`, 503 when master-data is unreachable; the old null-booking 500 is gone). Stock at UNKNOWN stays visible in the overview but is excluded from availability/ATP and can never be reserved (`UnknownLocationAllocationTest`). **Reporting aggregates**: `GET /api/inventory/reports/stock-by-sku` (available/allocated/unavailable per SKU; UNKNOWN-location stock counted unavailable) + `GET /api/inventory/reports/storage-density?days=90` (per-block daily occupied/total cells + pct from `storage_density_snapshot`; daily ShedLock-guarded sweep + on-demand snapshot for today).  **Stock follows the HU: bucket merge on collision at location bookings; HU-bound projection events book to the HU's current location, not the event's stale one.** |
| order-management | Java | 8084 | ✅ | Orders of all types (INBOUND/OUTBOUND/COUNT/ADJUSTMENT), lifecycle, release mgmt, dispatch service/route + ship-to + label-template (validated against master-data), line stock transactions via a local outbox → txlog (audit: actor required); delegates allocation. **Demo quick-seed** `POST /api/orders/demo/seed {type INBOUND/OUTBOUND, count?:10}` (demo-mode-only; outbound seeds are small single-line orders that fit one shipper; drives the demo-only "Add 10 Orders" buttons on inbound/outbound). **Order-flow report** `GET /api/orders/reports/flow?direction=INBOUND|OUTBOUND&days=90`: expected/active/started headline figures + zero-filled per-day (received/started/completed) and hour-of-day histograms; lifecycle mapping documented in `order-management.yaml`. **Short allocate and release** (`POST /api/orders/{id}/release-short`, `ORDER_RELEASE` = SUPERVISOR/ADMIN, actor logged): a NOT_FULFILLABLE order re-allocates allow-short and releases as `PARTIALLY_ALLOCATED` with per-line `allocatedQty` + `SHORT` statuses; 409 if not short or nothing available. Ship now accepts `PARTIALLY_ALLOCATED` and stages an order-level **OrderShipped** outbox event (per-line orderedQty/shippedQty/shortQty + shortShipped) for the host confirmation feed (`V9`: nullable `order_outbox.line_txn_id`). |
| allocation | Java | 8091 | ✅ | Pick-location allocation (UoM breakdown), cubing (APP multi-size largest-first / 1:1) with per-line carton traceability + per-carton dispatch labels (host barcode per shipper), batch picking. **Allow-short mode** (`allowShort` on `POST /api/allocation/orders`): a short order keeps what it could reserve, marks per-line shortfalls (`SHORT`, zero-stock lines fully short), cubes only the allocated quantities and returns `FULFILLABLE_SHORT` (`V5`); strict mode unchanged; batch picking accepts both reservation-holding statuses. |
| slotting | Java | 8093 | ✅ | Put-away assignment for automated rack/GTP blocks (weighted scorer: velocity-to-exit · same-SKU lane consolidation · aisle redundancy cap/floor · fill balance; exit distance = `location.distance_to_exit` when set, else derived from the cell coordinate (posX−1)+(posY−1)+(posZ−1) with the port assumed at position 1 ground level, and the guided rack builder stamps it on generation; soft single-SKU-per-lane (mixing penalty, outweighable by balance) + hard lane-capacity/max-aisle constraints; direct-to-pick), manual pick-face slotting + min/max replenishment (opportunistic off-peak top-off), off-peak re-slotting. Put-away candidates are filtered against live inventory occupancy (`POST /api/inventory/locations/occupied`) so physically-occupied slots are excluded before scoring (best-effort if inventory is down). **Profile-less SKUs fall back to the warehouse's only automated storage block** (SHUTTLE_ASRS/CRANE_ASRS/AUTOSTORE/AMR_GTP; ambiguous = several automated blocks still 400). Goods-in `assignPutaway` BPMN delegate. ADR 0003. **Relocation plan** (`POST /api/slotting/relocation-plan {warehouseId, locationId}`): for ADR-0009 multi-deep shuttle channels, plans the dig-out sequence before a blocked retrieve — ordered `{huId, fromLocationId, toLocationId}` steps for blockers occupying shallower cells (target: same `cellY` hard constraint, same aisle preferred; empty when the channel is clear); occupancy from the HU location registry, channel siblings from master-data cell coordinates (`RelocationPlanApiTest`). Move dispatch + txlog events pending. |
| gtp | Java | 8094 | ✅ | Goods-to-person station execution (ADR 0006): station + STOCK/ORDER node config, open order destinations (bind order HU + demand), present a stock HU → put-to-light put-list (greedy most-needed-first; one HU serves many orders = batch), confirm puts (full/short), complete destinations. ORDER_LOCATION (conveyor) + PUT_WALL (lit rack/AMR) destination topology share one engine. Orthogonal operating modes (PICKING / DECANTING / STOCK_COUNT / QC / MAINTENANCE): generalised work-cycle with mode-appropriate task lines + outcomes (decant-moves, count entries + variance, PASS/FAIL/HOLD verdicts, OK/DEFECTIVE/REPAIR checks). **Station inbound queue — ADR-0007 Phase 3c-1: source of truth relocated to flow-orchestrator** (`induction_queue_entry`). GTP's `POST /stations/{id}/queue` inbound enqueue is **deprecated** (counting no longer calls it); `POST /queue/{entryId}/complete` fans out to flow `POST /api/flow/induction/entries/{id}/done` and dispatches NO transport of its own — flow's return leg owns the store-back and only slotting picks the destination (gtp's parallel store-back + `SlottingClient` removed). Legacy `station_queue_entry` code kept but marked `@Deprecated`. **Deactivate/drain** (`POST /stations/{id}/deactivate|activate`): finishes queued work, rejects new HUs while draining; **`WorkplaceView` includes `acceptingWork`** so the console restores the drain-switch state on reload. **In-transit capacity caps** (`POST /stations/{id}/capacity`): max simultaneous inbound transports split by mode class (read by flow-orchestrator's `WorkplaceClient` to meter RETRIEVE dispatch). **Topology node sync** (`POST /stations/{id}/nodes/sync`): replaces STOCK/ORDER nodes from a topology projection — matched by code (preserves bound demand/HU), carries `inboundDistanceM` (feeding conveyor distance); called by flow-orchestrator on topology projection. `V7__station_node_distance.sql` adds `inbound_distance_m` to `station_node`. **Store-back ownership**: moved entirely to flow-orchestrator's return leg (slotting-only; no gtp-side STORE) — the old parallel dispatch double-stored totes. Seams: physical lights + picking-HU retrieval (adapters/flow-orchestrator), demand auto-wire from allocation, stock→txlog, decant→slotting-putaway, count→inventory-StockAdjusted — all follow-up. |
| counting | Java | 8095 | ✅ | Cycle/stock counting: count tasks (LOCATION/SKU/ZONE/BLOCK scope, BLIND/VARIANCE), ABC-cadence schedule generator, capture → variance vs inventory snapshot → within-tolerance auto-approve (posts `StockAdjusted`) / out-of-tolerance recount. **Delete OPEN tasks** (`DELETE /tasks/{taskId}`): removes a task + its lines while still OPEN; 409 once active. Seams (by id): GTP STOCK_COUNT station, cycle-count BPMN; adjustment via txlog. **Count-tote routing** (emulator mode, ADR-0007 Phase 3c-1): on count-task creation, automated-storage cells (`SHUTTLE_ASRS`, `CRANE_ASRS`, `AUTOSTORE`, `AMR_GTP`) are automatically routed to an active GTP STOCK_COUNT station via a single `flow.requestPresentation(...)` call — flow creates a `REQUESTED` induction entry and orchestrates the RETRIEVE + CONVEY journey itself, dispatching to the correct adapter family (`ASRS`, `AUTOSTORE`, or `AMR`). The cap is metered by flow at RETRIEVE dispatch; `REQUESTED` is uncapped so routing always succeeds at request time; best-effort (never breaks task creation). **Demo quick-seed** `POST /api/counting/demo/seed {count?:1}` (demo-mode-only; drives the demo-only "Add count task" button, one task per click). **At-station blind count** (`POST /tasks/{taskId}/lines/{lineId}/station-count {countedQty}`): blind state machine on `count_line` (`station_count_state`: PENDING → RECOUNT → ACCEPTED | ADJUSTED); two agreeing counts that differ from expected post a `StockAdjusted` (delta = counted − expected, reason `COUNTING`, actor from `X-Auth-User` / `?operator=`, fallback `"system"`); all-terminal lines → task `RECONCILED`. Schema: `V4__count_line_station_count.sql` + `V5__count_line_hu.sql` (`hu_id uuid` — tote snapshotted from inventory at task generation, carried on `StockAdjusted` events to target the correct tote bucket; null for bin stock). |
| txlog | Java | 8086 | ✅ | Append-only events + outbox + relay. |
| process-engine | Java | 8083 | 🟡 | Embedded Flowable BPMN: deploy definitions, start/inspect instances; service-task delegates originate WCS work (sample goods-in dispatches a device task). Process designer UI pending. |
| flow-orchestrator | Java | 8085 | 🟡 | Device-task lifecycle + **vendor-neutral conveyor routing** (topology graph w/ per-node hardware address, HU route plans, shortest-path next-hop, **loop capacity HOLD/OVERFLOW**, **topology learning** from observed scans). **Automation-topology placement** (levels, placed equipment w/ envelope + conveyor path/sections, connections, function points; `GET`/`PUT /api/flow/automation/topology`) drives the 3D/2D editor in `ui` and the routing-graph projection; placed **GTP workstations** carry a `station_id` link to their gtp_station. The projection **auto-infers connections from geometry** (equipment nodes within ~1.5 m are linked — no hand-drawn connections needed) and honours **explicit node-level connections** (`from_path_index`/`to_path_index` on `equipment_connection`, V18: stitches exactly the chosen path points at any distance and in BOTH directions (a connection links two systems; the direction of travel comes from each conveyor's own section edges), legacy connections fall back to exit/entry resolution; one dedup set across section edges + explicit + auto-inferred, so overlaps never project duplicate edges); function points can be placed **anywhere along a conveyor run** (split mid-section into named PLC node codes), and a divert/infeed stub that lands on another conveyor **merges onto it** automatically. GTP workstation role-interactions (STOCK/ORDER/DECANT, set in Properties) stay explicit. **Topology → GTP node projection**: on `POST .../topology/project`, each GTP workstation's STOCK/ORDER conveyor interactions are projected into the bound `gtp_station`'s nodes (`GtpClient.syncStationNodes`; best-effort — never aborts the routing projection). The 2D plan grid defaults to 1 m. **Flow-owned induction queue** (ADR-0007 Phase 3c-1): `POST /api/flow/induction/requests` (presentation request; flow orchestrates RETRIEVE + CONVEY, cap metered at RETRIEVE dispatch; `REQUESTED` backlog uncapped), `GET /api/flow/induction/queue?workplaceId=` (full `{REQUESTED,IN_TRANSIT,QUEUED}` pipeline, `arrival_seq` ordered), `POST /api/flow/induction/entries/{id}/done`. **Per-HU transport trace** `GET /api/flow/hu-trace?huId=`: append-only timeline (REQUESTED → RETRIEVED → INDUCTED → [RECIRCULATED/DIVERTED …] → ARRIVED → QUEUED → DONE); **Phase 3c-2**: conveyor decision points reported by the emulator (sorter `RECIRCULATED`/`DIVERTED`) are written to the trace before ARRIVED so the timeline explains arrival-order divergence (ADR-0007 R4). Schema: `induction_queue_entry` + `hu_transport_trace` (`V11__induction_queue_and_hu_trace.sql`). **Phase 3d ✅ (ADR-0008):** on each CONVEY dispatch (induction-outbound + return-to-storage leg) flow resolves the transport's entry/destination nodes via `TransportNodeResolver` and calls `assignRoute(huCode, [dest])`, putting `entryNode` in the task payload; the emulator live-walks when `entryNode` is present. `decide()` appends `SCANNED` trace rows (point = node code, decision = routed-to / held / completed) so the HU trace is a live material-flow timeline (`LiveScanConveyanceTest`). **Return-to-storage transport — slotting-only store-back:** workplace-done asks slotting (`POST /api/slotting/putaway`) for the destination; ONLY slotting slots a tote, the source slot is never a fallback. Answered → `storage_location_id` stamped, return CONVEY routed, arrival STOREs into the slotting-chosen location. Errored/no answer → the return CONVEY still goes out with NO destination + NO route plan (tote stays on the conveyor, entry flagged `awaiting_slot`, `V15`); a ShedLock-guarded sweep (`InductionSlotSweeper`, ~30s, `V16__shedlock.sql`) retries slotting and assigns destination + route mid-journey (per-scan routing; `SLOT_ASSIGNED` trace row) or fires the STORE directly when the plan-less CONVEY already completed. **HU bookings to operational locations**: CONVEY dispatch books the HU to the entry conveyor's operational location (master-data `GET /api/master-data/locations/operational?kind=EQUIPMENT&name=`, name = node code before `#`); QUEUED books the workplace's (`kind=WORKPLACE`, name = gtp station code via `WorkplaceClient.code`); STORE completion books the final slot; unresolvable → null/UNKNOWN, always best-effort. **ADR-0009 dig-out chain:** `dispatchRetrieve` calls `POST /api/slotting/relocation-plan`; when blockers exist, dispatches `RELOCATE` (or `BIN_RELOCATE` for AutoStore) for the front-most step, stamps `relocate_task_id` on the induction entry; callback books the blocker's new inventory location, writes a `RELOCATED` HU-trace row, then re-plans until channel clear → RETRIEVE goes out — stateless and self-healing. **Reporting aggregates** (`GET /api/flow/reports/{scan-quality,traffic,storage-movements,device-movements,transit-times}?warehouseId&days`, default 90 / cap 180 days, DEVICE_VIEW): `scan_stat` + `edge_traffic` daily counters (`V17__reporting_counters.sql`) bumped inside `decide()` by one atomic `INSERT … ON CONFLICT` upsert per scan/ROUTE answer (scans / no-reads / unknowns per scan point; traffic per directed edge incl. divert-default routes; a counter failure WARNs and never breaks the scan answer); storage-movements = completed STORE/RETRIEVE (+ AutoStore BIN_*) per payload `locationId` per day, device-movements = completed/failed device tasks per equipment per day (label: placed-equipment code → equipment id → family), transit-times = INDUCTED→ARRIVED per induction entry aggregated per day as count + p50/p95 ms (percentiles in Java); history accumulates from deployment day, forecasting happens in the UI; demo clear purges both counter tables. **Routing fast path (hard real-time, 300 scans/s @ ~10 ms budget)**: per-warehouse in-memory graph snapshot (`RoutingGraphCache`, immutable; nodes/adjacency/loop config) + reverse-Dijkstra next-hop tables memoised per target replace the per-scan full-edge fetch + Dijkstra; warm sync work = 1 indexed route-plan read (NOT cached: replica-shared dynamic state) + 1 route-position UPDATE (kept sync: loop occupancy + plan progression are read from it) + loop lock/count only on loop entry; counters + `SCANNED` trace move to a bounded in-process FIFO queue with one worker thread (`ScanSideEffects`; WARN-and-drop on failure); invalidation = topology PUT + routing projection (evict after commit) + defensive 60 s TTL. **`GET /api/flow/reports/decision-latency`**: {count, p50Ms, p95Ms, p99Ms, maxMs} over the last 4096 decisions, per instance (in-memory ring buffer); decisions over 10 ms WARN with phase breakdown. Sniffer capture front-end + BPMN origination pending. |
| iam | Java | 8087 | ✅ | Authorization model: users → roles → coded permissions; seeded roles; effective-permission resolution; **per-user warehouse access** (allowed warehouses + one default; admin-only writes; the gateway enforces warehouse scope). (Keycloak does auth.) |
| notification | Java | 8088 | 🟦 | — |
| integration-sap | Java | 8089 | 🟡 | Host gateway: per-shipper dispatch-label barcode (`POST /labels`) + route feed (`POST /routes/sync`) + SAP order/ASN intake (`POST /orders`,`/asns`) translated into the canonical Host API (materials resolved to SKUs). |
| integration-manhattan | Java | 8090 | 🟡 | Host gateway: Manhattan order/ASN intake (`POST /orders`,`/asns`) translated into the canonical Host API (items resolved to SKUs). |
| integration-host | Java | 8092 | 🟡 | Canonical vendor-neutral Host API (`/api/host/**`): orders + ASNs + **SKU sync (list of SKUs with UoMs + barcodes inline)** + inventory adjustments in; confirmations out via pull (cursor feed) **and** webhook push; idempotency keys. |
| equipment-emulator | Go | 9097 | 🟡 | Simulates all four device families (conveyor/ASRS/AMR/AutoStore commands); active when `HARDWARE_EMULATOR_ENABLED` is ON — flow-orchestrator routes device tasks here instead of the real adapters. Each command sleeps a realistic per-family/command duration before returning COMPLETED (result includes `durationMs`); `OPENWCS_EMULATOR_LATENCY_MS` overrides all commands (`0` = instant). **Fault injection** (`OPENWCS_EMULATOR_FAULT_RATE=N`): 1 in every N tasks returns FAILED (deterministic; result carries `fault: true`). **Loop recirculation** (`OPENWCS_EMULATOR_RECIRC_EVERY=N`): every Nth CONVEY recirculates once before diverting — arrival order visibly diverges from dispatch order (ADR-0007 R2); CONVEY result carries `recirculations` count + `decisions` list (sorter `RECIRCULATED`/`DIVERTED` points) written to the HU trace by flow (R4). **Live config** (`GET`/`POST /config`): latency, fault rate, and recirc rate are atomics changeable at runtime without restart (`{latencyOverrideMs, faultEvery, recircEvery}`). **Real telemetry** (`GET /state`): per-family `completed`/`failed` tallies from actual task load; snapshot echoes live config. **Live conveyor walk** (ADR-0008 Phase 3d-2, `walk.go`): a CONVEY task with an `entryNode` payload enters live-walk mode — fetches the routing topology from flow, then loops: `POST /api/flow/conveyor/route`, travel the replied edge at `0.5 m/s` (`speedMps` — live-tunable in `/config`), HOLD → dwell and rescan, COMPLETE → result callback; fails on NO_ROUTE/EXCEPTION; safety cap at 500 scans; `recircEvery` simulation does not apply to live walks; falls back to atomic sleep when no `entryNode`. `/config` gains `speedMps`; `walk_test.go` covers the scan loop, HOLD behaviour, and atomic fallback. **Decision-grade daily logs**: task accept/complete/fault/walk lines carry HU code, route, equipment and the why (recirculation policy, fault injection, hold reason, no-route rescans); `/config` changes log old -> new; degraded paths at WARNING with consequence. |
| adapters/conveyor | Go | 9091 | 🟡 | Health + stub loop; `POST /tasks` returns FAILED ("hardware not connected") — real-hardware seam. Emulation consolidated into `equipment-emulator`. Refusals logged at WARNING with HU code, route, equipment, reason and consequence. |
| adapters/{asrs,amr-geekplus,autostore} | Go | 9096, 9093, 9094 | 🟡 | Health + stub loop; `POST /tasks` returns FAILED ("hardware not connected") — real-hardware seam. Emulation consolidated into `equipment-emulator`. (asrs on 9096 — 9092 is Kafka's.) Refusals logged at WARNING with HU code, equipment, reason and consequence. |
| adapters/conveyor-sniffer | Go | 9095 | 🟡 | Ingests scan telegrams from defined source IPs (allowlist + decoder) → posts observations to the WCS for topology learning. Logs stream connect/close with per-session tallies, each forwarded scan (barcode + node + controller), and WARNINGs with the raw line for undecodable telegrams / lost observations. |
| ui | React/TS | 5173 dev / 443 prod | 🟡 | React/Vite SPA: **Keycloak login** + sidebar **app shell** + **dashboard** + **screen permission catalog** (role/user-gated nav & routes, overridable via Access control). Built screens: inbound, outbound, counting (in-app confirm dialog for OPEN-task delete), **GTP operator console** (single-active-session; **queue-driven + auto-present**: polls the station queue every 3 s and auto-presents the head QUEUED tote in PICKING mode — no manual form; **operating mode persisted per station** (localStorage — restored on reload); **per-mode accent hue** (counting orange, QC + maintenance red, picking/decanting green: mode pills + mode panel borders, via `modeAccent()` in `GtpOpsScreen.tsx` reusing the `--warning`/`--danger`/`--herbal-lime` tokens); **active-tote panel** shows a tote glyph + HU code hero, the SKU code wipth the SKU name on its own line, qty, SKU image and a metadata chip strip (base-UoM item dimensions/weight + `sku_profile` metadata via the one-call SKU card read, session-cached, never delaying the tote presentation; same identity block in the blind-count `CountPanel`); **fills the viewport when no cycle is active** (larger SKU image, centred; idle **waiting-for-totes** state also full-screen); queue surfaced as a right-side fold-out **drawer** (slim handle on right edge, slides open with backdrop) — now reads from `GET /api/flow/induction/queue?workplaceId=` and surfaces `REQUESTED` (in-storage, not yet retrieved), `IN_TRANSIT`, and `QUEUED` entries so operators see the full inbound pipeline; STOCK_COUNT mode also queue-driven — queue entries with `countTaskId`/`countLineId` trigger a **`CountPanel`** (blind: no expected qty; clears on RECOUNT, advances on ACCEPTED/ADJUSTED); falls back to "Done counting" button otherwise; **deactivate/activate** controls) + **GTP workplace config** (node locations searched by **location code**, not UUID; destination topology asked only for PICKING workplaces; **capacity configuration** for in-transit HU caps per mode class), **transport overview** (HU code column; origin/destination/next-hop resolve UUIDs to location codes via `useCatalog` or GTP station codes via `listWorkplaces`; fallback to short id; next-hop falls back to destination for ASRS/AMR/AutoStore direct-delivery tasks; **click-to-trace dialog**: click a row → detail + trace dialog with code-resolved fields; when the task has an HU id, shows the per-HU transport trace from `GET /api/flow/hu-trace?huId=` (REQUESTED → RETRIEVED → INDUCTED → ARRIVED → QUEUED → DONE timeline with point, decision, timestamps); falls back to the device-task correlation trace from `GET /api/flow/device-tasks?correlationId=` when no HU id is resolvable; **scope filter** replaces the old status filter: "Open + finished today" (default working-set), "Open (active) only", "Completed", "Failed", "All recent" — client-side on a 500-task backend window), stock transactions; **hardware twin** (`/hardware-twin`, Operations: live 3D digital twin — equipment coloured idle/running/faulted from device tasks, conveyor bodies tinted directly with their live state colour (green functional / orange jam or heavy traffic / red fault — no translucent overlay), totes moving smoothly (buffered scan-timeline interpolation rendered a few seconds behind real time on a data-anchored render clock that is immune to server/client clock skew, `hardwaretwin/motion.ts`) + ASRS cell positions, level selector + labels toggle); **automation topology** (React Flow; 2D plan editor: divert direction picker (L/S/R, min 2), double-click-to-draw corners, click-to-place ASRS IN/OUT, waypoint delete, click-select workstation conveyor function-points in 3D, **divert default direction** Select on a divert's function-point dialog (None (stop)/Straight/Branch, persisted with the topology); **routing graph tab**: generated routing graph as a read-mostly table inspector incl. each divert node's projected **default exit**; **route test mode**: pick start/target in the 3D scene — runs Dijkstra over the exact directed graph, renders the path + a 0.5 m/s ghost tote, or the reachable frontier when no path exists; **node-link visibility + explicit links** (`nodeLinks.ts`, pure client mirror of the projection's adjacency): live green/amber indicators in 3D + 2D where equipment meets (linked vs "not linked" with the gap in metres), a per-node **Connections section** in Properties showing each end node's counterpart (distance + auto/explicit) with closest-first link candidates, and Unlink with honest still-auto-linked feedback); BPMN designer (bpmn-js), slotting, master data (SKU/UoM/barcode read-only), settings (incl. a **Cubing** tab: edit cubing rules + full shipper CRUD with fill rates, and a **Hardware emulator** toggle), user management, access control, **warehouse access** (per-user allowed warehouses + default), **system info** (version, health **and logs** of every service/adapter via the gateway's `GET /api/system/services` + `…/{name}/logs?date=` + `…/log-days`; build-info stamped into each jar/adapter at build; per-service **daily log files**, 14-day retention, on a shared `openwcs-logs` volume — Java via libs:common logback, Go via a daily writer); **database console** (Administration → Database, `/admin/database`, ADMIN-only: schema → table tree, click-to-query (`select * … limit 100`), monospace SQL editor with Cmd/Ctrl+Enter, DataTable results with row count + truncated badge + execution time, PostgreSQL errors surfaced, last query in localStorage — SELECT-only backend). A global **top-bar warehouse switcher** auto-selects the user's default on login and scopes every warehouse-related screen (no UUID entry). A shared **`useCatalog`** hook resolves entity ids to human-readable codes across operations screens — counting (scope + capture dialog), outbound pick detail, stock-transaction From/To, and transport equipment column all display location codes, SKU codes (with description), and equipment codes rather than raw UUIDs; transport origin/destination/next-hop additionally resolve against GTP station codes (`listWorkplaces`). **Blind count** capture hides the Expected-qty column until reconciled. Inbound/outbound are **read-only** (host owns orders). Shared styled `Select` + searchable/sortable/paginated `DataTable`; warehouse-access user list paginates server-side (Keycloak). **Reporting** sidebar section (5 read-only screens, `/reporting/*`: material flow scan quality + scanner-attention prediction + 3D conveyor traffic heatmap + transit times; ASRS density 90d history + 14d weekday-seasonal forecast + 3D rack-cell movement heatmap + per-device movements; stock per SKU available/allocated/unavailable; inbound + outbound expected/started/active + 90-day dailies + hour-of-day peak maps; recharts charts, lazy 3D scene reusing the editor meshes). Per-screen **in-app help** rewritten to a task/worked-example/troubleshooting template for floor operators (`help/content.ts`). Dockerised (nginx) on host **:443 (HTTPS forced)** under compose `--profile apps` (proxies `/api`→gateway, `/realms`+`/admin`→Keycloak); **gateway JWT + warehouse scope enforced**. |
| libs/common | Java | — | ✅ | `EventEnvelope`. |

**Contracts:** OpenAPI ✅ master-data, inventory, txlog, allocation, order-management, iam,
flow-orchestrator, integration-sap, integration-manhattan, host-api, process-engine; ⬜ master-data
shipper/fulfillment-config paths, other services. Avro/Schema-Registry ⬜.

**Platform:** docker-compose ✅ (incl. allocation; Keycloak imports the `openwcs` realm).
**CI ✅** (GitHub Actions: Java build+test with Testcontainers, Go adapters, UI build, OpenAPI
validation). **Gradle wrapper committed.** **k8s starter manifests ✅** (`deploy/k8s/`; horizontal
scaling — see [`SCALING.md`](./SCALING.md)); Helm ⬜.

**Observability:** decision-point logging convention (INFO with codes next to ids and the
"because ..." trigger, WARN for skipped/degraded paths with the consequence, DEBUG in loops)
applied ✅ inventory, master-data, txlog; ⬜ remaining services.

---

## 2. Roadmap progress (build.md §15)

| Phase | Status | Detail |
|---|---|---|
| **0 — Foundations** | ✅ | Repo + compose + shared schemas + txlog/outbox/relay + Kafka ✅; IAM model + gateway JWT + per-endpoint RBAC (all services) + inter-service identity propagation ✅ (toggleable); **CI ✅ (green), Keycloak `openwcs` realm ✅, gradle wrapper ✅**; **JWT edge-auth path exercised end-to-end against a live Keycloak realm (Testcontainers) ✅**. Remaining hardening: mTLS between services. |
| **1 — Master data + inventory MVP** | ✅ | Master Data ✅, Inventory projection ✅, log→projection loop proven ✅. |
| **2 — Process engine + one equipment family** | ✅ | flow-orchestrator device-task lifecycle + uniform device contract ✅, conveyor adapter ✅, DEVICE RBAC ✅, **process-engine (Flowable BPMN) ✅ with a sample goods-in process that originates a device task** ✅. Gap: a process designer UI + richer processes. |
| **3 — Outbound + more equipment** | 🟡 | **order-management ✅, allocation + cubing + batch picking + release management ✅, inventory reservation/ATP ✅, dispatch labels/services/routes ✅ (incl. integration-sap label-barcode + route feed).** Gaps: host-integration gateways translate into the canonical Host API but the real SAP/Manhattan wire protocols (OData/BAPI/IDoc, Manhattan REST) are still skeletal ⬜; more adapters ⬜. The **BPMN outbound process** is now ✅ (process-engine: release → allocate → gateway on fulfillability → pick/dispatch → route). |
| **3b — Inbound slotting & replenishment** | 🟡 | **Put-away engine ✅ (velocity-to-exit, soft single-SKU-per-lane, aisle redundancy + balance), HU type capabilities + per-area allowed-HU-types ✅, empty-HU far placement + LOW transport priority ✅, multi-compartment HUs (dominant velocity + SKU-set affinity) ✅, cell-as-location coords aisle/side/x/y/z ✅, manual pick-face min/max + opportunistic replenishment ✅, off-peak re-slotting ✅, self-taught recency-weighted ABC ✅, slotting UI ✅, goods-in put-away delegate ✅, live-inventory occupancy filter on put-away candidates (drops physically-occupied slots before scoring; best-effort if inventory is down) ✅** (ADR 0003). **Relocation plan endpoint ✅** (ADR-0009 Phase 1: `POST /api/slotting/relocation-plan`, dig-out sequencing for multi-deep shuttle channels). **Profile-less put-away fallback ✅: a SKU without a storage profile resolves to the warehouse's only automated block** (`SHUTTLE_ASRS`/`CRANE_ASRS`/`AUTOSTORE`/`AMR_GTP`; several candidates still 400 — a profile must disambiguate). Gaps: physical move dispatch ⬜, txlog audit events ⬜, lane-depth ledger↔inventory reconciliation ⬜, goods-in decant step ⬜, deep-lane cell grouping ⬜. |
| **3c — Goods-to-person station execution** | 🟡 | **GTP station + node config, order-destination demand, present-stock → put-to-light put-list (batch: one stock HU → many orders), confirm/short puts, destination completion; ORDER_LOCATION + PUT_WALL topology ✅; orthogonal operating modes (PICKING / DECANTING / STOCK_COUNT / QC / MAINTENANCE) with a generalised task-line work-cycle + outcomes ✅; deactivate/drain control + re-activate ✅; in-transit capacity caps per mode class ✅; topology-projected station nodes (`POST /stations/{id}/nodes/sync`, `inboundDistanceM`) ✅** (ADR 0006). **Phase 3c-1 ✅: flow-owned induction queue** (`REQUESTED → IN_TRANSIT → QUEUED → DONE`, `arrival_seq`, cap metered at RETRIEVE dispatch; `induction_queue_entry` + `hu_transport_trace` in flow schema; `POST /api/flow/induction/requests`, `GET /api/flow/induction/queue`, `POST /api/flow/induction/entries/{id}/done`, `GET /api/flow/hu-trace`; GTP operator console shows `REQUESTED`/`IN_TRANSIT`/`QUEUED` pipeline; transport screen shows per-HU trace timeline; counting uses single `requestPresentation` call). **Phase 3c-2 ✅: emulator loop recirculation** (`OPENWCS_EMULATOR_RECIRC_EVERY=N`; `recirc.go`): every Nth CONVEY recirculates once, diverging arrival order from dispatch order (R2); CONVEY result carries `recirculations` + `decisions` (sorter `RECIRCULATED`/`DIVERTED`); flow's `onConveyCompleted` writes decision points to HU trace before ARRIVED (R4); `/config` gains `recircEvery` (live-tunable). **Phase 3d ✅ (ADR-0008): live scan-driven conveyance** — flow assigns route plans on each CONVEY dispatch + records `SCANNED` trace rows; emulator walks graph at 0.5 m/s obeying flow routing decisions; twin replays the real scan trail; return-to-storage CONVEY resolved via the routing graph (`LiveScanConveyanceTest`). **ADR-0009 ✅ (Phases 1 + 2): multi-deep channel relocation** — slotting plans the dig-out order (`relocation-plan`); flow dispatches RELOCATE chain, books + traces each blocker move, then issues RETRIEVE when the channel is clear (`RelocationPlanApiTest`). Gaps: physical put-lights + picking-HU retrieval (device adapters/flow-orchestrator) ⬜, demand auto-wire from allocation batches ⬜, stock decrement → txlog audit ⬜, decant→slotting-putaway + count→inventory-StockAdjusted wiring ⬜, gtp `station_queue_entry` table removal ⬜. |
| **4 — Counting & operations** | 🟡 | `StockAdjusted` projection ✅; **cycle/stock counting service ✅** (`counting`: count tasks, ABC-cadence schedule, blind/variance, recount + reconciliation → `StockAdjusted`); **Count-tote routing ✅** (emulator mode: counting calls `flow.requestPresentation`; flow orchestrates RETRIEVE + CONVEY to an active STOCK_COUNT station, dispatching to the correct adapter family — ASRS/AUTOSTORE/AMR; cap metered at RETRIEVE dispatch; best-effort); **GTP store-back family resolution ✅** (store-back STORE transport dispatched to the adapter family of the destination's storage type); dashboards/alerting ⬜; **at-station blind count UI ✅** (GTP ops `CountPanel` — GTP-routed totes only); **general operator count capture UI ✅** (`ui/src/counting/CountingScreen.tsx`: ad-hoc + scheduled count tasks, per-line count capture, variance review, recount, reconcile; count-tote routing status persisted with failed ASRS routing retried every minute). |
| **5 — Hardening & scale** | ⬜ | DLQs, circuit breakers, replay tooling, perf, security review. |

---

## 3. Test coverage (implemented; not yet run here)

| Service | Tests | Kind |
|---|---|---|
| master-data | `MasterDataPersistenceTest`, `MasterDataApiTest`, `MasterDataRbacTest`, `DispatchCatalogApiTest`, `LabelTemplateApiTest`, `HostManagedMasterDataTest`, `SkuSyncApiTest`, `EmulatorModeTest`, `AdminDbConsoleTest` | Testcontainers + MockMvc (incl. RBAC: read=VIEW, write=EDIT; shipping-service + route catalogs; label-template CRUD + ZPL/PDF render; host SKU sync upsert semantics (absent SKUs untouched; within a synced SKU nested UoMs/barcodes fully replaced); **hardware-emulator flag**: defaults OFF, enable/disable ADMIN-gated, `GET /emulator` reflects state; **admin DB console**: ADMIN gating, schema listing, SELECT happy path, UPDATE/INSERT/multi-statement/CTE-write rejection, row cap + truncated flag, smuggled write blocked by the READ ONLY transaction) |
| txlog | `TransactionLogServiceTest`, `OutboxRelayTest` | Testcontainers + Mockito |
| inventory | `InventoryPersistenceTest`, `StockProjectionServiceTest`, `InventoryServiceTest`, `OccupiedLocationsTest`, `HandlingUnitLocationTest`, `StockBySkuReportTest`, `StorageDensityReportTest` | Testcontainers (incl. per-location occupancy: a stock-row location and a handling-unit location both report occupied, an empty one does not; **HU location registry**: create, update, clear via `PUT /api/inventory/handling-units/{id}/location`; **stock-by-SKU report**: available/allocated/unavailable split incl. UNKNOWN-location stock, over-allocation floors at 0, answers without the split when master-data is down; **storage density**: idempotent per-block-per-day snapshot, on-demand today, window cut-off, zero-cell block) |
| allocation | `AllocationEngineTest`, `AllocationServiceTest` | Pure logic (incl. multi-size cubing: largest-first + line split across cartons with `lineNo`/`shipperUnitId` links) + Testcontainers (allocate → cancel releases reservations; oversized SKU → `CUBING_FAILED` + reservation released; per-carton dispatch labels with a host barcode per shipper; **allow-short**: available qty reserved + held, zero-stock line fully short, only allocated quantities cubed, strict mode still releases everything) |
| order-management | `OrderTransactionTest`, `OrderTransactionRelayTest`, `OrderAuthorizationTest`, `OrderReleaseShortTest`, `DemoSeedTest`, `OrderFlowReportTest` | Testcontainers + Mockito (outbox, relay, and per-endpoint RBAC: VIEWER 403 / SUPERVISOR 201; **release-short**: OPERATOR 403, transition to `PARTIALLY_ALLOCATED` with per-line short quantities, 409 when not short / nothing available, ship of a short order stages `OrderShipped` with shipped < ordered; **demo seed** creates 10 sample INBOUND/OUTBOUND orders when demo mode on, rejected when off; **order-flow report**: seeded INBOUND/OUTBOUND orders across statuses + backdated timestamps produce the documented expected/active/started figures, per-day buckets and hour-of-day intake) |
| iam | `IamServiceTest` | Testcontainers (seeded roles, effective permissions, catalog validation) |
| slotting | `RelocationPlanApiTest` | MockMvc + mocked clients (relocation plan: clear channel → empty steps; single blocker → one step with correct from/to; chained blockers → ordered steps all sharing the same `cellY`) |
| flow-orchestrator | `DeviceTaskServiceTest`, `RoutingEngineTest`, `RoutingServiceTest`, `DiscoveryServiceTest`, `InductionQueueServiceTest`, `LiveScanConveyanceTest`, `ReportingTest` | Testcontainers + Mockito (device tasks; pure next-hop; routing through targets; loop HOLD/OVERFLOW; topology learning; **`InductionQueueServiceTest`**: RETRIEVE callback → `IN_TRANSIT` + dispatches CONVEY; CONVEY callback → `QUEUED` with `arrival_seq` in arrival order (incl. out-of-request-order case proving R2); queue read includes `REQUESTED`; cap counts only `{IN_TRANSIT,QUEUED}`; one trace row per write point; DONE endpoint idempotent; **Phase 3c-2**: `conveyDecisionPointsAreWrittenToTheTrace` — emulator recirculation payload (`RECIRCULATED`+`DIVERTED` decisions) is written to the HU trace before ARRIVED; **Phase 3d**: `LiveScanConveyanceTest` — CONVEY with `entryNode` triggers route-plan assignment + `SCANNED` trace rows; scan-loop HOLD; return-to-storage CONVEY entry/destination resolved from projected graph; **`ReportingTest`**: scan/no-read/unknown counters accumulate into one row per node+day; ROUTE answers (incl. divert defaults) count edge traffic; day-window filtering; storage/device movements aggregated from seeded device tasks; transit-time p50/p95 from trace pairs; decision-latency ring-buffer percentiles after 200 decides; **`RoutingFastPathTest`**: graph-snapshot eviction on editor replace AND on topology projection (next decide routes the new graph); precomputed next-hop tables equal the old per-scan Dijkstra on a non-trivial graph (unique-path costs, all node pairs); async side effects eventually persist (polling assert); warm decide() p50 under 10 ms over 200 planned scans with measured numbers printed) |
| equipment-emulator | `tasks_test.go`, `walk_test.go` | Go httptest (`POST /tasks`: COMPLETED with realistic latency, simulated duration reported; **fault injection** — `faultEvery=2` fails every 2nd task with FAILED + `fault: true`, alternates deterministically; **loop recirculation** — `recircEvery=2` recirculates every 2nd CONVEY deterministically (`TestConveyJourneyRecirculatesDeterministically`); a recirculating CONVEY task reports `recirculations=1` + two-entry `decisions` ([RECIRCULATED, DIVERTED]) in the result payload (`TestConveyTaskReportsDecisions`); **`/config` endpoint** — POST updates `latencyOverrideMs`/`faultEvery`/`recircEvery`/`speedMps` atomics, GET reflects them; 405 on GET `/tasks`; **`walk_test.go`**: live-walk CONVEY scans every graph node and obeys flow's routing decisions; HOLD causes rescan of the same node; tasks without `entryNode` fall back to atomic sleep) |
| adapters/conveyor | `main_test.go` | Go httptest (`POST /tasks`: FAILED "hardware not connected" on all commands, 405 on GET) |
| adapters/{asrs,amr-geekplus,autostore} | `tasks_test.go` | Go httptest (`POST /tasks`: FAILED "hardware not connected" for all families) |
| adapters/conveyor-sniffer | `sniffer_test.go` | Go (decoder; IP allowlist; ingest→decode→forward end-to-end; HTTP observation post) |
| gateway | `GatewayAuthEndToEndTest` | Testcontainers (live Keycloak + imported `openwcs` realm): no token → 401, realm JWT → 200 + identity propagated, client-supplied `X-Auth-*` stripped (anti-spoof) |
| integration-sap | `LabelControllerTest`, `RouteFeedControllerTest`, `SapOrderControllerTest` | MockMvc (label-barcode; route-feed upsert; SAP order → Host API translation with material→SKU + unknown-material 422) |
| integration-manhattan | `ManhattanOrderControllerTest` | MockMvc (Manhattan order → Host API translation with item→SKU + unknown-item 422) |
| integration-host | `HostControllerTest`, `ConfirmationControllerTest`, `HostReferenceControllerTest`, `HostInventoryControllerTest`, `IdempotencyFilterTest`, `WebhookDispatcherTest` | Testcontainers + MockMvc + mocked clients (order/ASN mapping; confirmations cursor feed; SKU sync with UoMs + barcodes; adjustment → StockAdjusted append; `Idempotency-Key` replay; webhook push advances cursor) |
| process-engine | `ProcessEngineTest`, `OutboundProcessTest` | Testcontainers + Flowable (goods-in dispatches a device task; outbound releases → user task → dispatch, exercising delegates + a wait task; an `allowShort=true` instance short-releases and `FULFILLABLE_SHORT` flows the happy path) |
| gtp | `GtpContextTest`, `WorkCycleExecutionTest`, `OperatingModesTest`, `StationQueueTest`, `StationCapacityTest`, `StationExceptionTest` | Testcontainers (entity↔schema round-trip; one stock HU serves many destinations = batch; confirmations decrement + complete; short HU leaves surplus demand OPEN; short confirm → SHORT; BOTH topologies — ORDER_LOCATION + PUT_WALL — produce correct lit put instructions; **each operating mode** — DECANTING moves source→target + exposes put-away seam, STOCK_COUNT computes variance + exposes StockAdjusted seam, QC records PASS/FAIL/HOLD, MAINTENANCE records OK/DEFECTIVE/REPAIR; unsupported-mode rejected; setSupportedModes retains PICKING; **`StationQueueTest`** (ADR-0007 Phase 3c-1): completion fans out to flow `done` + store-back; drain rejection; **`setCapacity`**: caps reflected in station view; **`StationExceptionTest`**: store-back dispatches to the correct adapter family (`storeBackDispatchesToTheDestinationStorageFamily` — AUTOSTORE destination → `AUTOSTORE` asserted on `createTransport`; existing store-back test updated to stub `SHUTTLE_ASRS` → `ASRS`)) |
| counting | `CountingServiceTest`, `DemoSeedTest`, `CountRoutingTest` | Mockito + Testcontainers + mocked clients (`CountingServiceTest`: delete accepted for OPEN task, rejected with `IllegalStateException` once counted; **demo seed** creates sample count tasks over demo stock when demo mode on, rejected when no stock; **count-tote routing** (ADR-0007 Phase 3c-1): emulator ON + ASRS cell + active station → `flow.requestPresentation` called (one call; no separate gtp.enqueue or flow.createTransport); **multi-family routing**: AutoStore cell → `AUTOSTORE` family asserted on `requestPresentation`; emulator OFF → no routing; routing failure → count task still created; **at-station blind count** (4 tests): first count == expected → ACCEPTED no adjustment; two agreeing counts ≠ expected → ADJUSTED (delta posted, reason COUNTING, task RECONCILED, **`huId` asserted on adjustment — targets tote bucket not phantom HU-less row**); third count after two mismatches → ADJUSTED; recount == expected → ACCEPTED without adjustment) |

---

## 4. Known gaps, caveats & tech debt

- **Uncompiled** — biggest caveat; run the test suite before trusting any of it.
- **`ddl-auto: validate` + JSONB** — usually fine on Hibernate 6; fallback is
  `ddl-auto: none` (tests still cover mappings).
- **No auth** anywhere.
- **Cubing** is volume+weight greedy (not 3D); it packs across multiple shipper sizes
  (largest-first, downsizing the last carton) but doesn't optimise for fewest cartons.
- **Pick-type breakdown** assumes base-UoM stock and reads case size from the "CASE" UoM;
  SPLIT_CASE is treated as eaches for quantity.
- **Allocation↔services** calls are synchronous REST; partial-failure compensation exists
  in the allocator but cross-service consistency is best-effort (no saga/outbox there yet).
- **Audit `actor`** is required on stock transactions + every logged event, but is
  caller-asserted until IAM/JWT supplies an authenticated principal.
- **Dirty-tote vs flow's return leg**: marking a tote dirty (maintenance) still triggers flow's
  return-to-storage leg — gtp can no longer (and never effectively could) suppress it; a
  "complete without return" flag on flow's `done` endpoint is a follow-up.
- Order status is not auto-advanced by postings (no auto-complete on `postedQty` ≥ `qty`).
- Events only on `txlog.stream`; no consumer-driven contract tests (CI does validate the
  OpenAPI specs structurally). First CI run may surface compile errors not catchable locally.

---

## 5. Suggested next steps

1. **Stand up a Keycloak `openwcs` realm** + `gradle wrapper` so the JWT + RBAC path is
   exercisable end-to-end (enforcement is wired across all services but only verifiable with a
   realm). Consider resolving custom IAM roles at runtime and mTLS between services.
2. **End-to-end MockMvc tests** across the outbound slice (release → allocate → ship → cancel)
   and the inbound/count/adjust posting + relay flow.
3. **master-data catalog events** + shipper/fulfillment-config paths in `master-data.yaml`.
4. **Order auto-complete** when a line is fully posted (`postedQty` ≥ `qty`).
5. **process-engine (Flowable BPMN)** + goods-in/outbound processes that drive the
   flow-orchestrator device tasks (Phase 2 increment 2).

> Done since last revision: **Phase 2 increment 1** — flow-orchestrator now owns the device-task
> lifecycle over the uniform device contract (build.md §8): `POST/GET /api/flow/device-tasks`,
> the `flow.device_task` store, family→adapter routing via `HttpDeviceClient`, and DEVICE_VIEW /
> DEVICE_OPERATE RBAC. The **conveyor adapter** gained a `POST /tasks` simulator. `DeviceTaskServiceTest`
> (Testcontainers) and `main_test.go` (Go) added; `flow-orchestrator.yaml` OpenAPI spec added.
> The device contract is synchronous HTTP for now; async Kafka (`device.tasks`/`device.results`)
> is the production target.
