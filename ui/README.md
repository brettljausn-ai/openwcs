# openwcs UI

React + TypeScript + Vite SPA. Serves operator (legibility-first) and
management (full design system) screens. Visual tokens come from
[`styling.md`](../styling.md); see build.md §11.

- **Run:** `npm install && npm run dev` (proxies `/api` to the gateway on :8080)
- **Build:** `npm run build`
