import ComingSoon from '../shell/ComingSoon'
export default function CountingScreen() {
  return <ComingSoon title="Stock counting" note="Cycle / stock counting — tasks, capture, variance and reconciliation." />
}
