{
  "name": "openwcs-public-site",
  "version": "1.0.0",
  "private": true,
  "description": "openWCS public product site — Express + EJS, deployable to Node hosts (Hostinger).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:pages": "node scripts/convert.js",
    "build:static": "node scripts/build-static.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-ejs-layouts": "^2.5.1"
  },
  "devDependencies": {
    "cheerio": "^1.0.0"
  }
}
